---
title: FA-001～FA-006 运行证据（R-002 验证包）
status: active
created: 2026-07-21
updated: 2026-07-21
parent: GOAL-009-ai-assisted-governance-workbench
version: 0.1.0
type: verification-evidence
package: r-002-verification-package.md
package_version: 0.1.0
---

# FA-001～FA-006 运行证据（2026-07-21）

## 环境

| 项 | 值 |
|----|-----|
| 日期 | 2026-07-21 |
| OS | Windows |
| Python | 仓库根 `.venv`（与既有 web 测试一致） |
| 包 | [r-002-verification-package.md](r-002-verification-package.md) v0.1.0 |
| 实现 | `web/services/fact_admission.py`（纯函数，**无** canonical I/O、**无** AI 运行时） |
| 测试 | `web/tests/test_fact_admission.py` |

## 命令与结果

```text
cd web
../.venv/Scripts/python.exe -m unittest discover -s tests -q
----------------------------------------------------------------------
Ran 68 tests in ~2s
OK (skipped=1)
```

基线对照：A-030 时代 **61 passed, 1 skipped**；本拍新增 FA 相关 **7** 个用例 → **68 passed, 1 skipped**。

## 分项证据

| ID | 测试方法 | 触发 | 预期拒绝码 / 行为 | 结果 | canonical 写入 |
|----|----------|------|-------------------|------|----------------|
| FA-001 | `test_fa001_missing_source_kind_blocks_confirm_and_proposal` | `source_kind=None` / 空白 | `ERR_FA_MISSING_SOURCE_KIND`；`can_confirm`/`can_request_proposal` false | **pass** | 无（纯函数） |
| FA-002 | `test_fa002_missing_source_statement_blocks_confirm_and_proposal` | `source_statement=None` / `""` | `ERR_FA_MISSING_SOURCE_STATEMENT` | **pass** | 无 |
| FA-003 | `test_fa003_ai_origin_disguised_as_user_provided_rejected` | `user-provided` + AI/tool 信号 | `ERR_FA_SOURCE_KIND_DISGUISE`；诚实 user/ai 可通过 | **pass** | 无 |
| FA-004 | `test_fa004_unconfirmed_candidate_in_canonical_zone_unlabeled` | 未标候选的 Candidate 进 `canonical_fact` zone | `ERR_FA_RENDER_CANONICAL_UNCONFIRMED` | **pass**（**view-model 契约**） | 无 |
| FA-005 | `test_fa005_stale_digest_after_source_change_rejected` | 内容变更后仍用旧 digest | `ERR_FA_DIGEST_STALE`；新 revision+digest 可通过 | **pass** | 无 |
| FA-006 | `test_fa006_source_kind_mutation_same_revision_after_submit_rejected` | submitted 后同 revision 改 `source_kind` | `ERR_FA_SOURCE_KIND_MUTATION`；新 revision / draft 可变 | **pass** | 无 |
| （正例） | `test_happy_path_user_provided_confirm_allowed` | 合法 user-provided | 允许 confirm / proposal_requested | **pass** | 无 |

## FA-004 范围说明

本拍 FA-004 验证的是 **RenderItem view-model 契约**（`validate_render_contract`），不是浏览器 DOM/E2E。  
全量候选面板 UI 合并前仍建议保留 residual **R-F002-1**（若走有界关 F-002 路径 A）。

## 未覆盖（本拍）

- FA-007～FA-012（AI/工具运行时）  
- AI broker / 密钥加载  
- 将 admission 门禁挂入 HTTP 路由（可选后续）  
- 关闭 F-002 的正式审计声明（须另令 `/govern 审视关闭 F-002`）

## 声明

核心 FA-001～FA-006 在纯函数层 **全部 pass**；**不**自动关闭 F-002；**不**将 I-002/I-008 标 `verified`（须编排响应与用户确认）；**不**扩展 α 写动作集。
