I'll run a read-only audit dispatch probe: load the audit skill, resolve the independent-audit prompt path, then inspect GOAL-008 for source/status semantics and a current evidence gap.Next I'll read the independent-audit prompt and GOAL-008's current meta/info requirements to identify source/status rules and a live required evidence gap.Confirming the current unreleased-candidate matrix so the evidence gap is accurate, not stale.**GROK_AUDIT_DISPATCH_OK**

### 1. Marker
`GROK_AUDIT_DISPATCH_OK` (emitted after repository reads only; no file writes, no audit append, no subagents, no questions).

### 2. Loaded skill
| Field | Value |
|------|--------|
| **Title** | `audit · 独立交叉审计（Grok Build skill）` |
| **Actual source path** | `C:\Users\magicvr\Documents\Code\goal-governance\.grok\skills\audit\SKILL.md` |

### 3. Independent-audit prompt named by that skill
| Field | Value |
|------|--------|
| **Named path** | `<SKILLS_PKG>/prompts/05-independent-audit.md` |
| **Resolved SKILLS_PKG** | `skills/` (contains `prompts/05-independent-audit.md` and `prompts/00-govern-orchestrator.md`) |
| **Actual path** | `C:\Users\magicvr\Documents\Code\goal-governance\skills\prompts\05-independent-audit.md` |
| **Prompt title** | `05 · 独立交叉审计（交叉入口核心）` |

### 4. Required source/status semantics + current required evidence gap

**Source / status semantics** (from loaded skill + `05-independent-audit.md` hard constraints):
- **`source` must be `independent`**
- **Default: audit opinions only** → target `03-audit.md` (optional attachments)
- **Must not** change goal `status` / `progress` / decision body / goal-tree status columns (unless explicit written “边审边改” authorization this turn; default refuse)
- Responses, finding closure, and stage advance belong to **`/govern`**
- Vision-layer independent review is **`/vision-audit`**, not `/audit`

**One current required evidence gap** (GOAL-008, live matrix — not the historical v0.7.0 close-out):
- Historical close-out remains **`done / 100%`** for annotated `v0.7.0`.
- Current matrix is **`candidateRevision: unreleased`** (`docs/contracts/skills-consumer-compatibility-matrix.json`).
- **Gap:** GitHub Copilot CLI `1.0.75` **`/vision`** is still **`pending-runtime-validation`** with **`evidence: []`**. Fresh passing host runtime evidence is required before treating the unreleased candidate as runtime-complete / release-ready.
