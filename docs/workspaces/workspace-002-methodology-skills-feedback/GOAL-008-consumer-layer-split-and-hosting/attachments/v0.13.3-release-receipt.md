---
id: GOAL-008-consumer-layer-split-and-hosting
doc: attachment
title: v0.13.3 发布凭据（merge / tag / workflow）
status: recorded
parent: GOAL-001-methodology-skills-feedback-evolution
created: 2026-09-13
updated: 2026-09-13
version: 0.1.0
---

# 附件 · v0.13.3 发布凭据（2026-09-13）

> 用途：为 A-001 F-005（I-006 发布产出）提供可核对的发布事实。**资产与结论以 tag workflow 的实际结果为准**；workflow 未完成前，本附件只登记已发生事实，不宣称发布成功。

## 1. 代码与 tag 身份

| 项 | 值 | 核对方式 |
|----|-----|----------|
| PR | **#21**（`dev` → `main`） | `gh pr view 21` |
| PR CI | `contract-and-report` pass + `windows-install-surface` pass（run `34748398218`） | `gh pr checks 21` |
| merge commit | **`dfa8600ae638657d084098a48f6e8c4a7457a24a`**（`Merge pull request #21 from magicvr/dev`） | `git log --oneline -1 origin/main` |
| annotated tag | **`v0.13.3`** → `dfa8600…`（`git cat-file -t v0.13.3` = `tag`，`^{commit}` = `dfa8600…`） | 已核对 tag 对象类型为 annotated 且指向 main merge commit（符合 D-011 §1） |
| tag 推送 | `git push origin v0.13.3` 成功 | — |

## 2. Tag workflow

| 项 | 值 |
|----|-----|
| workflow | `Skills pack (tag)` |
| run | **`34748633773`**（event `push`，ref `v0.13.3`） |
| 结构 | `pack` job（测试 + 打 zip + 上传 artifacts）→ `publish` job（**Environment `release` 审批** + wait timer + release evidence 硬门禁 + `gh release create`/upload） |
| 状态 | 登记时**排队中**；`publish` 需要仓库所有者在 GitHub 上**手动审批**（AI 无法代签） |
| URL | https://github.com/magicvr/goal-governance/actions/runs/34748633773 |

## 3. 发布后必须核对的资产（形态沿用 v0.13.2）

| # | 资产 | 核对动作 |
|---|------|----------|
| 1 | `goal-governance-skills-0.13.3.zip` | 重下载，sha256 与 `.sha256` sidecar 比对 |
| 2 | `…zip.sha256` | 字节内容与本地重算一致 |
| 3 | `goal-governance-core-0.13.3.zip` | 同上 |
| 4 | `…zip.sha256` | 同上 |
| 5–9 | compatibility report / release evidence / 校验文件 | 以 workflow 实际上传清单为准，**不得事后补记** |

## 4. 发布前已完成的门禁（本地 + CI）

| 门禁 | 结果 |
|------|------|
| docs / skills / scripts 全量测试 | 65 / 44 / 128 全通过 |
| 镜像 stage `--check` | 37 对 0 漂移 |
| `git diff --check` | 洁净 |
| `compatibility_report --require-ready` | 通过（`ready-for-release-evidence`） |
| release rehearsal（local + Windows CI） | 通过 |
| 12 格 runtime 证据 | `docs/releases/runtime/v0.13.3/` 全部 pass，`--check` 12/12 |

## 5. 未完成（诚实登记）

- **Environment `release` 审批尚未发生** → `publish` job 未运行 → **Release 资产尚未上传**；
- **资产 sha256 逐项核对未执行**（依赖上一项完成）；
- 因此：**A-001 F-005 仍为 open**，GOAL-008 **不得标记 `done`**，也不得宣称 `v0.13.3` 已发布（`CHANGELOG` 与 `docs/README` 目前均标注「未发布/发布候选」，待资产核对后再更新为已发布事实）。
