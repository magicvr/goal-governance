---
id: A-016
goal: GOAL-008-consumer-layer-split-and-hosting
doc: audit-entry
record_id: A-016
source: self
auditor: 编排主线程 /govern
scope: v0.13.3 正式发布验收 · A-001 F-005 闭合判定
verdict: pass
status: recorded
parent: GOAL-001-methodology-skills-feedback-evolution
created: 2026-09-13
updated: 2026-09-13
version: 0.1.0
---

# A-016 · v0.13.3 发布验收与 A-001 F-005 闭合（2026-09-13）

## 头字段

- **source**：self
- **auditor**：编排主线程 `/govern`
- **类型 / scope**：release · 发布产出核对与 A-001 F-005 闭合
- **verdict**：pass

## 范围与区间

- covered：merge/tag/workflow/资产/摘要/包内容；发布身份与公开台账一致性。
- excluded：consumer 侧真实安装（另见隔离仓附件）；宿主机行为（另见层语义与 `/commit` 探针附件）。
- 发布范围依据 [D-011](../01-decision/D-011-s6-release-scope-freeze.md)（I-006 冻结）。

## 成果与证据（逐项可核对）

| 主张 | 证据 |
|------|------|
| PR #21 合入 `main` | merge commit **`dfa8600ae638657d084098a48f6e8c4a7457a24a`**；`origin/main` HEAD 即此 |
| annotated tag 指向 merge commit | `git cat-file -t v0.13.3` = **`tag`**；`v0.13.3^{commit}` = `dfa8600…` = `origin/main` |
| tag workflow 全绿 | run **`34748891084`**：`pack` **success**、`Publish GitHub Release (gated)` **success** |
| Environment 审批 | `pending_deployments` 显示 environment `release`、reviewer = 仓库所有者、`wait_timer: 5`；审批后 `started_at 09:10:01`，Release `published_at 09:11:04` |
| Release 已创建 | `https://github.com/magicvr/goal-governance/releases/tag/v0.13.3`，`target_commitish: main` |
| 资产清单 | **9 项**（与 v0.13.2 形态一致）：两 zip + 两 sidecar + `bootstrap-README.md` + `install-online.{ps1,sh}` + `compatibility-report.json` + `release-evidence.json` |
| 摘要核对 | 重下载全部 9 项 → `goal-governance-skills-v0.13.3.zip` 与 `goal-governance-core-v0.13.3.zip` 的 sha256 **与 sidecar 逐项一致** |
| 包内容核对 | zip 内含 `agents_merge.py`、`core/docs/templates/vision/roadmap.md`、四宿主 `commit` 壳（claude/grok/codex/copilot）、`install.sh`；`install.sh` **无 BOM**、以 `#!/usr/bin/env b…` 开头 |
| 公开台账一致 | `CHANGELOG.md`「Unreleased」= 空、`0.13.3 - 2026-09-13` 段在位；`docs/README.md`「最近发布基线」含 **`v0.13.3`** 且写明 tag/workflow/资产核对事实 |

## A-001 F-005 闭合判定

| 要求（A-001 原文 / D-011） | 本轮产出 | 结论 |
|---------------------------|----------|------|
| 发布版本 | `0.13.3`（CHANGELOG + docs 台账） | 满足 |
| tag / revision 关系 | annotated `v0.13.3` → main merge commit `dfa8600` | 满足 |
| 资产清单 | 9 项已上传，逐项列出 | 满足 |
| 回归矩阵 | docs 65 / skills 89 / scripts 130 全绿；镜像 0 漂移；`--require-ready` 通过；rehearsal 通过 | 满足 |
| cross 覆盖面 | self（A-004～A-009、A-014～A-016）+ independent（A-001、A-003、A-007、A-013，provider = grok build） | 满足 |
| consumer vs producer 证据归属 | consumer 侧：隔离仓冷启动/升级/legacy 附件、宿主探针附件；producer 侧：12 格 runtime 证据、compatibility report、release evidence、pack 摘要 | 满足 |

**判定：A-001 F-005 `fixed`（闭合）。** 发布产出全部可核对，且公开台账与 git/Release 事实一致。

## 过程事实（一并留痕）

1. **发版曾中断一次**：编排器误判 legacy 迁移会丢消费方内容 → 用户裁决停发 → 删除 tag（**从未发布、无资产**）；随后编排器自查更正（不变量成立、代码未改），见 [halt-and-misdiagnosis 附件](../attachments/v0.13.3-halt-and-misdiagnosis-correction.md)，并在同一 commit `dfa8600` 上**重新打 tag**。
2. **`main` 上出现 4 次 `workflow_dispatch` 的 pack 运行**：由仓库所有者**手动触发**（非 push 自动触发）。该 workflow 的 `on:` 只有 `push: tags: v*` 与 `workflow_dispatch`；`workflow_dispatch` 默认 `publish_release=false`，4 次运行全部 `conclusion=success` 于 `pack` 阶段，且**未创建任何 Release**（Release 仅由 tag push 的 run `34748891084` 的 gated publish 创建）。运行 provenance 已用 `gh api .../workflows/skills-pack-release.yml/runs` 核实。
3. **首个 tag run `34748633773`**：在 runner 排队期间 tag 被删除，从未执行；无需处理（如需要可在 Actions 页面删除该 run）。

## 结论与下一步

**verdict：`pass`。** 发布产出完整可核对，A-001 F-005 闭合。GOAL-008 的全部开放 required 至此为 **0**。

**下一步**：S6 关门评估——检查目标成功标准（FB-006～FB-009）逐条证据，确认无未合法闭合的 required finding 后，将 GOAL-008 标为 `done / 100%`，并同步 `goal-tree.md`（树 + 表）。
