---
id: E-006
goal: GOAL-006-consumer-surface-convergence
doc: execution
title: 执行 F-003：governance_root≠docs 消费场景 e2e 测试
status: recorded
created: 2026-08-08
updated: 2026-08-08
version: 0.1.0
---

# E-006 · F-003 执行事实（2026-08-08）

## 事实

用户 `/govern` 指令：「执行 GOAL-006 F-003：补 governance_root≠docs 消费场景 e2e 测试（install 到非 docs 根 + 读取路径断言）」。

- `skills/tests/test_consumer_surface_relativeization.py` 新增 **`ConsumerSurfaceE2ETests`**（3 条，post-close 维护轮）：
  1. `test_pinned_agents_reads_paths_under_non_docs_root`——模拟消费仓（`.goal-governance.json` 配置 `governance_root: governance`）；AGENTS.md = 模板 + `{{GOVERNANCE_ROOT}}` pin 展开为 `governance`；断言无裸 `docs/` + 关键协议路径读取于配置根。
  2. `test_installed_shells_read_paths_under_governance_root`——四入口壳无裸 `docs/` + 含 `{governance_root}/` 引用。
  3. `test_all_path_references_resolve_under_configured_root`——全部路径引用解析于配置根、无 `docs/` 泄漏。
- **F-003（independent，low）fixed**；A-001 R-002 并入闭合（A-004 响应记录）。
- 验证：本文件 **8 passed, 90 subtests**；全量 **242 passed, 4 skipped, 98 subtests**（239 + 3 新增，无回归）；未改 canonical 白名单 → 无需 stage。

## Checkpoint

- 提交 hash 见本轮 commit 记录；owned paths = `skills/tests/test_consumer_surface_relativeization.py`、A-004、`03-audit.md` 索引、E-006、`02-execution.md` 索引、Root 02-execution 波次表。未用 `git add -A`。

## 下一步（待用户）

1. 其余登记项触发未到：GOAL-002 I-003（发版宣称）、GOAL-003 F-003（Web writer）、GOAL-006 R-001（release 轮）。
2. GOAL-006 全部 finding（F-001～F-004）已响应完毕。
