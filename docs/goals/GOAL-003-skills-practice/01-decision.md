---
id: GOAL-003-skills-practice
doc: decision
status: done
parent: GOAL-001-main-vision
created: 2026-07-18
updated: 2026-07-18
version: 0.2.0
---

# 决策记录 · GOAL-003

## D-004 · 单一主入口编排；01～04 降为原语（2026-07-18）

**决定**：Skills 的**默认用户路径**改为**一个编排入口**（核心提示词 `00-govern-orchestrator` + Copilot `/govern` wrapper 等安装接线）。编排器必须：

1. 扫描项目与 `goal-tree`，分类情境（无未关门总目的 vs 存在未关门目标等）；
2. 无未关门总目的时：引导用户表述清楚第一个/下一个总目的，再创建/更新 Root 类目标；
3. 有未关门目标时：分析树，提议下一步（进一步拆解 / 决策 / 执行 / 阶段或关门审计），**经用户确认**后调用对应原语协助；
4. 在用户视角维护流程上下文（以 `docs/goals/` 为真相源），不要求用户先选「填哪张表」。

`01-create-new-goal` / `02-record-decision` / `03-update-execution` / `04-write-audit`（及旧四 slash）定位为 **primitives / advanced**：供编排器调用或熟练用户直调，**不再**作为并列主产品面。

**为什么**：

- GOAL-001 要求 Skills **推进目标**，不是只保证四类 Markdown 写对。
- A-003 已记录：四入口填表形态相对核心预期漂移；继续优化「少问字段」无法纠正产品模型。
- 单一入口降低认知负担；生命周期状态机与文档原语解耦，规则层（五件套、P-001、goal-tree）可复用。

**未选方案**：

| 方案 | 未选原因 |
|------|----------|
| 保持四个并列 primary slash 为默认入口 | 用户仍须自判流程位置；继续文档中心而非目标中心 |
| 删除 01～04 只留自然语言 | 原语保证写入结构一致；编排器需要可调用子程序 |
| 新开 GOAL-005 而让 003 保持 done | 会掩盖阶段 2 未完成；应回退 003 进度后纠正再关门 |
| 做独立 Agent 二进制 | 超出本目标范围（提示词/规则/安装物即可验证行为契约） |

## D-001 · 优先完善 Skills，再推进 Web 数据能力

**决定**：在 GOAL-002 初始化完成后，下一阶段优先完善 Skills（规则模板、提示词、目标文件夹示例）并在本项目中实践验证，而不是先做 Web 数据模型 / CRUD / 校验工具。

**为什么**：

- **文档是 source of truth**：Web 与 AI 都依赖同一套目标文档；规则不清时，先做界面只会放大漂移。
- **协作立刻受益**：人与 AI 每天都在写 meta / decision / execution / audit；更清晰的 Skills 能马上降低结构错误与虚构进度。
- **成本更低、闭环更快**：完善模板与提示词可在文档侧快速迭代；Web 联动依赖稳定约定，过早开发易返工。
- **GOAL-002 审计已指向此方向**：bootstrap 收尾明确建议用新子目标承接 Skills 深化，避免继续堆在初始化目标里。
- **实践验证才算交付**：仅有空模板不够；在本项目强制使用并留下反馈记录，才能证明 Skills「可执行」。

**未选方案**：

| 方案 | 未选原因 |
|------|----------|
| 先做 Web CRUD / 读 `docs/goals` | 规则与模板未打磨时，数据映射与 UI 字段容易反复改 |
| 先做自动化校验工具 | 校验依赖约定稳定；当前更缺「怎么写」而不是「怎么查」 |
| 先做可安装 Skill 包 | 打包前应先把内容与工作流跑通，否则安装物会过早固化半成品 |
| 把 Skills 工作继续塞进 GOAL-002 | 会模糊「初始化完成」边界，与 bootstrap 成功标准冲突 |

## D-002 · 本目标交付物形态

**决定**：以「可复制文件 + 使用反馈文档」为主交付，不绑定特定 IDE 安装格式。

**为什么**：

- 与 GOAL-002 已建立的 `skills/` 结构一致，便于其他项目复制启用。
- 反馈记录沉淀在本目标（文档或 `attachments/`），形成可审计的改进闭环。
- 为后续「可安装 Skill 包」子目标保留清晰前置条件。

## D-003 · 本仓库强制使用与模板同步

**决定**：本仓库继续以根目录 [AGENTS.md](../../../AGENTS.md) 为生效规则；`skills/AGENTS.template.md` 为可复用模板。规则变更时两者应对齐，并在执行记录中注明。

**为什么**：

- 避免「模板改了、仓库实际规则没改」的双源不一致。
- 实践验证发生在本仓库，必须先保证本地规则可执行。
