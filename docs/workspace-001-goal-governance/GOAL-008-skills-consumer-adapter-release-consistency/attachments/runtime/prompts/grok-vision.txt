/vision scan

This is a read-only runtime dispatch probe. Do not modify files, launch subagents, or ask the user questions. Load the installed vision skill and follow its scan phase only. Read the minimum repository sources needed to report:

1. literal marker GROK_VISION_DISPATCH_OK;
2. the loaded skill title and actual source path;
3. the vision orchestrator prompt path named by that skill (06-vision-orchestrator);
4. active Charter vision_id@version and whether workspace-001 primary_plan resolves.

Only emit the marker after performing the repository reads. A prompt echo is not sufficient.
