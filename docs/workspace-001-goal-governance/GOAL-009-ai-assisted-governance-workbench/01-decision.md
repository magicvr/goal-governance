---
id: GOAL-009-ai-assisted-governance-workbench
doc: decision
status: done
parent: GOAL-001-main-vision
created: 2026-07-20
updated: 2026-07-22
version: 1.0.0
---

# 决策记录 · GOAL-009

## D-001 · 以人机协作工作台而非只读浏览器为产品方向（2026-07-20）

**状态**：accepted

**确认来源**：用户明确要求 Web 服务于“人类工作时有 AI 协助”的目标治理，而非完善纯只读页面。

**决定**：

1. GOAL-009 规划的产品是人类主导、AI 协助的治理工作台。AI 应把 canonical 目标记录转化为有引用的上下文、候选下一步、候选文档变更和风险提示；人保有审查、编辑、拒绝、确认和裁决权。
2. 受控写入属于产品方向，但不是本轮实现承诺。任何写入须经过结构化提案、可读 diff、明确确认、协议验证、可恢复事务和可追溯的审计证据。
3. **原则**：当前工作区的 canonical 根与其中 `goal-tree.md` 是唯一运行时真相。工作台可消费和受控更新它们，但不建立第二套状态、生命周期或“AI 已完成”标记。
   - **路径澄清（D-009 / A-014 F-024）**：本条起草时仓库仍使用全局 `docs/goals/`。GOAL-011 迁移后，本工作区 canonical 路径为 `docs/workspace-001-goal-governance/`；原则不变，实现与文档不得再硬编码已废弃的全局路径。
4. 每个 required 信息项只阻断其影响的阶段：I-001 阻断产品方案冻结，I-002 阻断 AI 接入，I-003/I-004 阻断首个 Web 写入，I-005 阻断部署或外部访问，I-006 阻断端到端验收。它们不阻断本目标的产品发现，也不要求机械等待所有项关闭后才可按已明确的最小范围立项。

**为什么**：

- 读取 Markdown 本身不能替代目标设立、决策、执行、审计与响应工作；用户需要的是更好的治理动作能力。
- 现有文档协议和 `GoalsRepository` 的事务基础可以成为可信协作面的起点，但尚不足以安全地暴露给人或 AI。
- 将人类确认和证据作为产品核心，既避免 AI 自主越权，也避免“只读”成为逃避设计真实工作流的借口。

**未选方案**：

- 把阶段 6 收敛为树浏览、Markdown 呈现和诊断打磨：价值与直接阅读文档重叠，不能推进实际治理。
- 直接把现有 service 写方法暴露为 Web/AI API：当前缺少身份、授权、确认、溯源、并发和安全失败机制。
- 让 Web 或模型维护独立状态：会违背 canonical 文档真相源并造成同步漂移。

**影响与后续**：本决定引用根 D-014。I-001～I-006 的收集结果将决定第一个实现目标的边界；本轮不宣称任何产品功能或安全控制已完成。路径表述的澄清见 D-009，不改写本条其他原则。

## 信息需求与阶段门禁

本目标的唯一信息台账位于 [00-meta.md](00-meta.md#信息就绪与未知项)。实施、部署和验收门禁以该表为准；没有 residual risk 接受。

## D-002 · 将第一阶段限定为单用户、多工作区、事实确认的工作台（2026-07-20）

**状态**：accepted（第 5 项已由 D-004 限定/替代）

**确认来源**：用户在本目标的独立审计请求中明确给出 Web 第一阶段的产品边界，并授权在不符合时修正计划。

> **共享资料区子范围限定**：本条第 5 项及其相关理由/未选方案由后续 [D-004](#d-004--重新确认共享资料区边界并限定-d-002-资料区子项2026-07-20) 限定和替代；D-002 的其他条目仍然有效。

**决定**：

1. 第一阶段面向一名用户，不考虑多用户、协作、账号、角色或权限管理。目标负责人、审计等保留为治理工作流的语义视角，而不是系统中的人或授权角色；P-004 仍由这名用户作出明确裁决。
2. 事实的准入权在用户。AI 可以在用户要求或确认的范围内调用网络工具、本地自动化或知识检索，也可以基于已确认信息提出推导；但这些结果必须以候选形式显示来源、时间和引用或推导链，并经用户显式确认后才可写成 canonical 事实、证据或风险结论。没有来源或确认的内容不得由 AI 编撰为事实。
3. 工作台应自动复用本工作区已确认的 canonical 信息。纯读取或计算视图可明确标为“计算得出”而不重复向用户索取文本；任何要进入长期记录的新增事实仍须经过第 2 项的确认链。
4. 一个工作区对应一个独立根目标和 canonical 范围。工作区之间不能读取、写入或混合 AI 上下文；平台级工作区索引仅用于导航，不得维护第二套目标状态。具体存储拓扑、根目标命名空间和生命周期仍由 I-009 收集后冻结。
5. **历史原文；共享资料区子范围已由 D-004 限定/替代，非当前用户事实**：贡献资料区在工作区之外，允许用户上传并让多个工作区只读引用特定版本。工作区可写自己的资料引用、注释和已确认的派生记录，但不能修改资料原件、资料版本或其他工作区内容；具体资料治理由 I-010 决定。该原文中由错误术语衍生的上传、只读、资料原件和版本不可修改等描述不再作为当前产品边界；共享资料区的有效事实与规则以 D-004 为准。
6. 第一阶段的信息架构至少包括总览页、工作区列表页和以目标树为核心的工作区详情页。详情页必须能通向本工作区的事实确认、资料引用、门禁、审计和受控变更；具体首个用户旅程由 I-011 审视。

**为什么**：

- 这组边界让工作台先减少单个用户治理闭环中的重复输入与上下文切换，而不提前引入多租户、协作权限或跨工作区同步复杂度。
- 将“AI 候选”和“用户确认的事实”分开，才能同时利用检索/自动化能力和保持 `docs/goals/` 事实记录可信。
- 多工作区与共享资料区是不同的数据边界：前者要求目标状态与上下文隔离；后者解决共享信息/资料需求，但不得成为读取、推理混合或写入其他工作区的通道。单一用户对共享资料区的 CRUD 权与其版本/引用治理规则以 D-004 为准。

**未选方案**：

- 用负责人、协作者、审计者建成第一阶段的多角色权限模型：与单用户范围冲突，并会在未验证价值前引入不必要的身份与授权设计。
- 让 AI 搜索、知识回答或推导自动写入事实：会把未验证信息伪装为用户确认的记录，违背本项目的证据与 P-005 门禁。
- 让所有工作区共同使用一份可写目标树，或让某个工作区绕过共享资料区管理边界修改另一工作区的状态：会破坏根目标隔离与写入可追溯性。该限制不否定单一用户在共享资料区内的全权 CRUD。

**影响与后续**：

- 本决定修正 `00-meta.md` 中原先“负责人、协作者和审计者”的多角色表述，并新增/细化 I-008～I-012。
- 它只确定产品边界，不宣称工作区、资料区、AI 检索、确认链或页面已实现；I-001～I-006、I-008～I-011 仍为 collecting 的 required 信息。
- 本轮独立审计的 findings 仍须由 `/govern` 汇总、响应并以可核对证据关闭；本决定不改变目标 status 或 progress。

## D-003 · 将 R-001 组织为待用户审视的单用户三页最小旅程（2026-07-20）

**状态**：proposed

**确认来源**：用户要求优先收集 I-001/I-011，并先形成单用户工作流、三页信息架构和最小垂直旅程。

**决定**：

1. 将 I-001 与 I-011 作为同一 R-001 收集项处理，产出一份可审视的候选设计，而不是分别零散记录页面或功能清单。
2. 候选信息架构固定为三个顶层页面：总览、工作区列表、以目标树为核心的工作区详情。事实候选、共享资料区查看/管理、门禁、审计和受控变更均是工作区详情中的上下文面板或动作入口，不额外宣称为顶层页面。
3. 候选最小垂直旅程限定为：进入一个既有工作区的目标树 → 选定目标并基于 canonical 上下文形成候选事实 → 用户编辑、拒绝或要求形成受限变更提案 → 查看受影响 canonical 记录的 diff 与门禁 → 经明确确认后显示受控结果和证据。该旅程只界定产品意图；确认状态机、基线绑定、operation id、并发/幂等和实际写入协议仍由 I-003/I-004/I-006 收集，不能据此开放写入。
4. 设计稿必须明确单一用户、无账号/协作/角色模型、无 AI 自动写入或裁决、无跨工作区上下文混合，以及 D-004 已确认的共享资料区边界；具体 UI、数据模型、访问/操作契约仍不在本稿冻结。

**理由**：

- A-004 的 R-001 要求把 F-001/F-005 的工作流与三页旅程形成可由用户审视的共同证据；将两项分离会丢失页面导航、候选确认与最小场景之间的依赖。
- 以既有工作区中的一次窄事实确认作为候选首切片，可验证工作台是否减少上下文切换，而不会把多工作区、资料库、AI 提供方和完整写入系统一次性当作首个实现范围。
- 将结果保持为 `proposed` 可避免把本轮设计输入误写为用户已接受的产品方案、路线图 A 退出或 required finding 的关闭证据。

**未选方案**：

- 直接把现有只读首页和单目标详情命名为三页工作台：当前没有工作区选择、确认或受控变更入口，且不能满足 F-005 的审视要求。
- 将事实确认、资料引用、门禁、审计和受控变更各自扩展为独立顶层页面：会在尚未验证首个闭环前扩大导航和实现范围。
- 以 AI 自动抓取或直接写入作为最小旅程：会越过用户触发、候选呈现、显式确认和 I-003/I-004 的开放门禁。

**影响与后续**：

- 候选设计见 [R-001 收集稿](attachments/r-001-single-user-workflow-ia-vertical-journey.md)。I-001/I-011 保持 `required / collecting`，直至用户审视并留下可核对结论；F-001/F-005 仍开放。
- 不改变 GOAL-009 的 `status`、`progress` 或路线图阶段；不立项实现、不修改 `web/`，也不把该文档当作 I-003/I-004/I-006 的确认或写入契约。

## D-004 · 重新确认共享资料区边界并限定 D-002 资料区子项（2026-07-20）

**状态**：accepted

**确认来源**：用户明确确认“贡献资料区”为“共享资料区”的笔误，并在 A-005 响应期间确认：一期必须具备共享资料区以解决多个工作区共享信息/资料的问题；资料由用户上传，AI 助手可自行读取；单一用户可对共享资料全权 CRUD；工作区彼此不可见，且每个工作区只可见自身和共享资料区。用户随后明确接受本轮由 AI 提出的四项候选规则。

**决定**：

1. 正确术语为“共享资料区”。D-002 第 5 项以及以错误术语衍生的“只读贡献资料”“资料原件不可修改”等描述，不再作为用户确认事实或后续门禁基线；D-002 在单用户、事实确认、多工作区隔离和三页信息架构上的其余决定继续有效。
2. 共享资料区是第一期必备能力，用于共享信息/资料。用户上传资料并可全权新增、查询、修改和删除；AI 助手可在既定 AI 边界内自行读取。该读取不授权 AI 自动写入、把资料内容当作 canonical 事实，或在未获允许时调用外部工具。
3. 工作区彼此不可见；每个工作区只可见自身和共享资料区，且不得通过共享资料区访问、推理混合或写入其他工作区。
4. 用户接受以下共享资料治理规则：资料版本化并以哈希固定引用；工作区本地注释或派生内容必须指向固定的原资料版本；删除前必须警示受影响引用并保留可追溯历史；敏感、不可信或受版权约束资料仅按数据处理，AI 不执行其中指令且不自动外传。
5. 这些规则是确认的产品边界，不是已实现或已验证的技术契约。I-010 继续为 `required / collecting`，直至共享资料模型、元数据、版本/哈希和引用机制、用户 CRUD/AI 读取边界、删除历史、安全处理及跨工作区正反访问测试具备可核对证据。
6. 本决定不自动建立“工作区彼此不可见”的平台级导航例外。总览/工作区列表能否显示其他工作区的最小导航元数据，以及允许的字段范围，仍由 I-009/I-011 经用户审视后决定；在此之前不得将 R-001 的跨工作区摘要候选当作已确认能力。

### 受影响位置清单与处理

| 分类 | 受影响位置 | 处理 |
|------|------------|------|
| 纯术语替换 | `00-meta.md` 的成功标准、I-011、R-001 页面标签与说明 | 改用“共享资料区”；不借替换推定旧语义继续有效。 |
| 用户重新确认并已接受的有效边界 | `00-meta.md` 的工作流、I-010、边界；R-001 的资料区输入 | 写入本决定的用户事实和四项接受规则，并保留 I-010 的验证状态。 |
| 必须撤回或重写的 AI 衍生语义 | D-002 第 5 项及相关“只读贡献资料”“工作区不得修改资料原件/版本”的陈述；执行记录的“共享只读贡献资料区”；R-001 对资料区的“只读引用”前提 | 标记 D-002 子项已被限定，改为本决定的共享资料边界；不把旧陈述当作当前用户事实。 |
| 历史审计记录 | A-001～A-006 中的旧术语和衍生语义 | 保留历史，使用 A-007 记录仅在共享资料区子范围的限定/替代关系，不静默改写。 |

**为什么**：正确术语和可追溯的用户确认是 GOAL-009 的事实准入前提。用户有全权管理共享资料区，并不改变工作区间的隔离；版本固定、注释溯源、删除追溯和安全规则让共享资料不会成为不可核对的跨工作区状态通道。

**未选方案**：

- 仅机械将“贡献资料区”替换为“共享资料区”：会继续把旧 AI 推断伪装为用户事实。
- 因用户确认了产品规则而将 I-010/F-004 视为已验证或开放实现：仍缺模型、契约和负向访问/安全证据。
- 以一个工作区的上下文或操作权限代替共享资料区管理：会破坏工作区隔离。

**影响与后续**：本决定是 A-005/A-006 的 canonical 响应依据。它修订 `00-meta.md`、本文件、`02-execution.md` 与 R-001，并由 A-007 限定 A-001～A-004 的共享资料区子范围；不改变 GOAL-009 的状态、进度或路线图阶段。

## D-005 · 暂缓数据库选型，并约定 AI API 使用本地环境配置（2026-07-20）

**状态**：accepted（第 1 项由 [D-011](#d-011--产品打包与-dogfood-分栏并确认导航存储f-005-与-sqlite-立场2026-07-21) **部分限定**：首切片仍不引入 DB；后续 Web 结构性非 canonical 索引允许 SQLite）

**确认来源**：用户明确要求当前阶段暂不考虑数据库；AI API 的参数配置暂定放在 `.env`，并要求提供范例文件。

**决定**：

1. 当前 GOAL-009 的收集和 **D-006 首垂直切片**不引入或依赖数据库作为写入路径。工作区 canonical root、目标五件套和 `goal-tree.md` 继续是唯一目标状态真相。**D-011**：产品更广阶段可将 SQLite 仅用于可重建的结构性非 canonical 数据；不得用 DB 权威保存目标生命周期状态。
2. 后续服务端 AI API 配置统一约定为 `web/.env`；仓库跟踪不含真实凭据的 `web/.env.example`。范例只声明启用开关、提供方、基础 URL、API 密钥、模型和可选请求参数的字段名；本阶段不提供页面设置入口。
3. 真实 API 密钥只可保存在本机未提交的 `web/.env`，不得写入仓库、Markdown canonical 记录、模板、浏览器响应、页面、日志或审计附件。当前只读 Web 尚未加载该文件，也未调用任何 AI 提供方。
4. 提供方、模型、可见上下文、工具同意、参数语义、配置加载方式、密钥权限/轮换、部署和故障降级仍分别受 I-002、I-005、I-006 约束；本决定不关闭任何 required 信息项或 finding。

**为什么**：在尚未冻结 AI 协作和部署契约前，先统一本地配置入口可避免把真实凭据写入 canonical 文档或前端，同时不把未审视的数据库、AI 提供方或安全实现误写为既成能力。

**未选方案**：

- 现在引入 SQLite 或其他数据库：会超出用户指定的当前范围，并可能掩盖 I-009/I-010 的开放存储与隔离设计问题。
- 将 API 密钥写入 Markdown、源码、`web/.env.example` 或浏览器设置页：会泄露凭据，并混淆 canonical 记录与运行时秘密。
- 仅提供范例文件就宣称 Web 已支持 AI：当前没有 `.env` 加载器、AI broker 或任何 API 调用，不能作此声明。

**影响与后续**：更新 I-002、I-005 与 I-009 的已知边界，并新增 `web/.env.example` 和使用说明。数据库选型的复核触发为拟引入非 canonical 应用状态、立项多工作区产品实现或首次部署；I-002、I-004、I-005、I-006、I-009、I-010 及 F-003/F-004/F-007/F-008 保持原状态，GOAL-009 的状态、进度和路线图阶段不变。

## D-006 · 收敛首个垂直切片与设计契约（2026-07-20）

**状态**：accepted

**确认来源**：用户明确接受对 GOAL-009 的收敛建议。

**决定**：

1. 首个要验证的产品价值切片限定为一个显式选定的既有工作区中的既有目标：用户提供一条候选执行事实，经审视后只可形成“向该目标的 `02-execution.md` 追加一条用户确认执行事实”的受限提案。它不同时承担创建目标、关闭 finding、工作区创建、跨工作区导航或任意 Markdown 编辑。
2. 首个切片以工作区详情为主界面，目标树为核心导航。总览和工作区列表仍是后续信息架构候选，但在 I-009 未决定最小导航元数据、索引边界和隔离策略前，不实现为跨工作区功能。D-003 的三页描述因此仅保留为产品发现输入，不再被当作首切片的实现清单。
3. 在任何 Web 写入前，先为 `Candidate`、`Proposal`、`Confirmation` 和 `ExecutionReceipt` 四类对象定义可审查契约：候选必须绑定来源与工作区/目标范围；提案必须绑定目标文件、canonical 基线、diff、门禁快照和有效期；确认必须绑定不可变的提案摘要；结果必须关联 operation id、实际写入、审计证据和失败/恢复结果。这是 I-003/I-004/I-006 的设计收集，不是现有实现或放行。
4. 首个切片只接受用户提供的候选输入，不接入 AI、网络或本地工具调用。AI 协作与共享资料区仍是第一期产品方向，但分别受 I-002/I-008 和 I-010 的契约、边界与验证门禁约束；不得因其不在首切片中而取消或视为已完成。
5. 本决策只收敛范围并响应 recommended F-009。I-001/I-011 仍为 `required / collecting`，F-001/F-005 及所有其他 required finding 保持开放；GOAL-009 保持 `active / 0%`，不立项实现、不开放写入或 AI 服务。

**为什么**：现有 R-001 已指出首个切片不能被多工作区、共享资料、AI 提供方和完整写入系统同时拖宽。先固定一条受限的人类治理动作，才能把后续确认、事务、审计与失败安全契约做成可验证对象，而不是把页面或仓储层基础误作产品能力。

**未选方案**：

- 同时实现三页导航、多工作区索引、共享资料 CRUD、AI 工具与受控写入：会越过 I-002/I-003/I-004/I-006/I-009/I-010 的开放门禁，并失去首个可验证范围。
- 只实现只读浏览器而不定义候选到提案的受限动作：不能验证工作台是否改善实际治理。
- 直接把现有 `GoalsRepository` 写方法暴露给页面：缺少基线绑定、确认、operation id、幂等、审计关联和失败安全语义。

**影响与后续**：更新 `00-meta.md` 的首切片边界及 I-001/I-011 证据、更新 R-001 和执行记录，并在 A-010 响应 F-009。下一步是收集四类对象的字段、状态机和负向验证要求；在此之前不创建实现子目标。

## D-007 · 接受 R-004/A-011 的首切片设计裁决包（2026-07-21）

**状态**：accepted

**确认来源**：用户在 A-011 的 P-004 裁决点明确选择跳过 R-004/A-011 同范围 self 审视，并确认接受本轮逐项裁决包。

**决定**：

1. 本轮不新增同范围 self 审视。A-011 是针对 R-004 的独立审计，现有 A-003 与 A-010 不覆盖该 scope；用户已按 P-004 明确选择基于 A-011 进入响应。本条是编排响应依据，不将 A-012 伪装成同范围 self 审计。
2. 接受 F-013 的线性对象流：CandidateRevision、Proposal、UserDecision 和 ExecutionReceipt。UserDecision 是 D-006/R-001 所称 Confirmation 的受限用户决策对象；确认与执行在同一受控请求中衔接，审视中/等待确认中只作 UI 计算态。
3. 接受 F-014 的三层门禁：产品实现门禁继续阻止当前 Web/AI 写入能力；运行时只检查受限追加所需的操作策略；治理推进门禁不得借追加事实修改状态、关闭 finding 或放行阶段。
4. 接受 F-015 的追加与不变性约束：语义上只追加执行时间线，物理上以单文件 transactional replace 写入 02-execution.md；pin 该文件全文以及目标 meta、工作区 goal-tree 的 must-unchanged 摘要，禁止写入集扩大或更改 status/progress/parent/id。
5. 接受 F-016 的 B 方案：未来首切片使用工作区根下非 canonical 的 ops/receipts 旁路日志保存 receipt 关联；它不保存目标状态，首切片不自动清理。receipt 或 canonical pre/post digest 不可复核时必须 fail closed，不显示成功。
6. 接受 F-017～F-019：每工作区单写者串行化与乐观基线检查并用；本机 loopback 单用户 trust_context 不得外溢到外部访问；执行事实采用规范化、结构化且禁止治理推进指令的内容契约。
7. 上述为已接受的设计约束，不是实现或验证结论。F-007/F-008 保持 open，I-003/I-004/I-006 保持 required / collecting，且没有 residual risk 接受、Web/AI 写入放行或目标状态/进度变更。

**为什么**：A-011 已把 R-004 的风险定位到状态机可实现性、门禁语义、确认 diff 与实际写入一致性、receipt 权威性、并发、信任边界和内容可审计性。用户裁决使这些设计取舍成为可追溯约束，同时保留后续契约测试和独立复审所需的证据门槛。

**未选方案**：

- 保留四套细粒度持久状态机或把确认和执行拆成两次用户动作：会扩大基线漂移窗口并与 R-001 的线性旅程脱节。
- 将任意 open finding 直接作为禁止如实记录执行事实的运行时规则：会混淆产品实现门禁、操作策略与治理推进。
- 把 receipt 写入 canonical 五件套、现在引入数据库，或采用进程内临时 receipt：分别会形成第二状态源、越过 D-005，或不能满足可恢复的操作核对。

**影响与后续**：已按本决定修订 R-004，并在 A-012 记录 A-011 的响应与 finding 关闭范围。I-003/I-004/I-006 的信息表仅补充设计收集证据，不改为 verified；下一步仍是单独收集和验证契约、负向案例与实现证据，不创建或开放写入能力。

## D-008 · 将 R-004 负向案例转为契约测试计划（2026-07-21）

**状态**：accepted

**确认来源**：用户确认采用 `/govern` 提议的下一步：把 D-007/R-004 的负向案例整理为可审视的契约测试计划和实现前门禁清单。

**决定**：

1. 新增 [R-004 负向契约测试计划与实现前门禁清单](attachments/r-004-contract-test-plan.md)，以 CT-001～CT-015 覆盖来源与范围绑定、写集限制、基线/摘要漂移、确认有效期、幂等/重放、并发、恢复、receipt 核对、内容契约、治理推进边界和外部信任边界。
2. 每个案例都以“触发条件、拒绝/冲突或 fail-closed 结果、canonical 写入效果、关联 I-00N/F-00N”定义，作为后续契约测试的可审视输入；计划中的案例尚未执行，不得称为测试通过或关闭证据。
3. 将 I-003/I-004/I-006 与 F-007/F-008 作为实现前门禁保留：在 required 信息达到 `verified` 且 finding 有后续审视关闭证据前，不开放 Web/AI 受控写入，不创建运行时 `ops/receipts/`，不进入实现验收。

**为什么**：D-007 已接受 R-004 的设计取舍，但 A-012 明确这些取舍不是实现或运行时证据。把负向路径先整理为可核对矩阵，可以让后续实现目标逐项验证确认绑定、失败安全和操作身份，而不把设计稿、现有 `GoalsRepository` 或文档计划误作能力。

**未选方案**：

- 立即实现 Web/AI 写入并在实现中补测试：会越过 I-003/I-004/I-006 及 F-007/F-008 的 required 门禁。
- 只在 R-004 正文保留散列的负向描述：缺少测试 ID、断言、写入效果和证据记录格式，无法形成可执行验收范围。
- 先创建运行时 `ops/receipts/` 或数据库：本轮没有实现目标和运行时授权，会提前引入第二状态源或不可核对操作记录。

**影响与后续**：本决定新增 R-004 测试计划收集稿，并由 `02-execution.md` 记录产物、由 A-013 记录本次编排响应；GOAL-009 保持 `active / 0%`，I-003/I-004/I-006 保持 `required / collecting`，F-007/F-008 保持 `open / required`。下一步是审视测试 fixture、契约接口和运行证据格式，之后再按 P-001 另立实现子目标。CT 边界补全与规格包下沉见后续 [D-009](#d-009--响应-a-014并补齐-ct边界规格包与并行收集排期2026-07-21)。

## D-009 · 响应 A-014：并补齐 CT 边界、规格包与并行收集排期（2026-07-21）

**状态**：accepted

**确认来源**：用户以 `/govern` 明确指令响应 A-014：先处理 P-004（同范围 self 审计），再补 F-020/F-023（CT 边界 + 规格包），并行 F-022（R-001/R-002/R-003），可选 F-021/F-024；在 F-005/F-007/F-008 关闭前不立项实现、不开放写入。

**决定**：

1. **P-004**：对 A-014 先完成同范围 self 审视（A-015），再合并响应；不跳过 self，也不把编排响应伪装为 independent。
2. **F-020**：修订 [R-004 测试计划](attachments/r-004-contract-test-plan.md)，显式编号 CT-016（open required finding 下受限追加且 finding 仍 open）、CT-017（UTF-8/LF 规范化后 digest）、CT-018（affirm 与执行同请求，禁止无重校验第二步写入口）。仍为计划，不是测试通过。
3. **F-023**：新增 [最小可执行契约规格包](attachments/r-004-executable-contract-spec.md)，覆盖 fixture 布局、service-level 操作入口、错误/结果码、receipt 字段示例与 digest 算法说明；状态为 `pending-user-review`。规格审视 ≠ 测试通过 ≠ 写入放行。
4. **F-022 并行排期（书面保留）**：
   - **R-001**：继续收集/完成用户对 I-001/I-011 与 F-001/F-005 的可核对审视证据；不得仅凭 R-004 深度退出路线图 A 或立项实现。
   - **R-002**：下一收集动作 = 起草事实准入与 AI 协作契约（I-002/I-008/I-006 相关）；首切片不调用 AI/工具，但其门禁在路线图 B 前仍 required，不得 silently 当作已关闭。
   - **R-003**：下一收集动作 = 在 GOAL-010/011 输入之上形成工作区产品映射与共享资料 CRUD/访问契约草案（I-009/I-010）；首切片不实现跨工作区导航或资料区，门禁保留。
   - **硬禁令**：F-005 仍 open 时禁止创建实现子目标；F-007/F-008 仍 open 或 I-003/I-004/I-006 未 `verified` 时禁止开放 Web/AI 写入。
5. **F-021（可选，本轮处理）**：在 R-001 §2.1 固定历史命名 → D-007/R-004 §6 优先命名映射；实现与 fixture 以 CandidateRevision / UserDecision 为准。
6. **F-024（可选，本轮处理）**：在 D-001 第 3 点注明原则为“工作区 canonical 根 + goal-tree”，当前路径为 `docs/workspace-001-goal-governance/`；不改写历史审计正文。
7. 本轮不调整 GOAL-009 的 `status`/`progress`，不创建实现子目标，不创建运行时 `ops/receipts/`，不将任何 I-00N 标为 `verified`，无 residual risk 接受。

**为什么**：A-014 确认纪律良好但 required 门禁仍全开；其 F-020/F-023 阻断把 CT 计划误当作可测规格，F-022 阻断用 R-004 深度绕过旅程与工作区/资料门禁。用户给定顺序与硬禁令与 A-004 响应组及 P-001/P-005 一致。

**未选方案**：

- 跳过 A-014 同范围 self：与本轮用户 P-004 选择不符。
- 仅补 CT 编号不写规格包：F-023 仍开放，实现者仍会自创接口语义。
- 立即另立实现子目标或开放写入：F-005/F-007/F-008 仍 open。
- 把 R-002/R-003 标为 verified 或 residual：无新证据，违反 P-005。

**影响与后续**：A-015 记录 self；A-016 记录 finding 响应与关闭范围；`02-execution` 记录产物；I-003/I-004/I-006 证据栏补充计划/规格包路径但仍为 collecting。下一步：用户审视规格包与 CT-016～CT-018；并行推进 R-001 审视结论与 R-002/R-003 收集稿；在 F-005 关闭前不立项实现。

## D-010 · 接受 R-004 规格包、I-001/I-011 审视结论并启动 R-002/R-003 收集（2026-07-21）

**状态**：accepted

**确认来源**：用户以 `/govern` 明确指令：审视规格包 `r-004-executable-contract-spec.md` 与 CT-016～CT-018 并给出接受/修订意见；对 I-001/I-011 给出可核对审视结论（瞄准 F-001/F-005）；起草 R-002 与 R-003 收集稿；硬禁令保持——F-005 关闭前不立项实现；F-007/F-008 关闭且 I-003/I-004/I-006 `verified` 前不开放 Web/AI 写入。

**决定**：

1. **R-004 规格包与 CT-016～CT-018**：**接受为实现前契约/测试规格基线**，并纳入三项小修订（已写入规格包 v0.2）：  
   - 幂等重放（CT-007）规范为 `result=committed`（或原终态）返回既有 receipt、不重复写入；不以错误码表示成功重放；  
   - `CandidateRevision`/`Proposal`/进行中决策草稿仅进程内存或非 canonical ops，不得写入五件套；  
   - 补充 CT-016 案例 JSON 最小示例。  
   接受 **≠** 测试执行、**≠** I-003/I-004/I-006 `verified`、**≠** F-007/F-008 关闭、**≠** 写入放行。
2. **I-001 / F-001**：根据 R-001 §7.1 的可核对对照，**关闭 F-001**（单用户无角色边界 + 页面场景已满足 A-001 关闭要求）。I-001 保持 `required / collecting`（长期成功标准/可用性仍可收集），不标整项 `verified`。
3. **I-011 / F-005**：根据 R-001 §7.2，**F-005 保持 open / required**。首切片旅程与详情 IA 已可核对，但三页 IA 与 I-009 导航例外未决；关闭路径为 α（书面收窄 F-005 到详情切片）或 β（完成总览/列表审视）。**在 F-005 关闭前禁止创建实现子目标。**
4. **R-002 / R-003**：新增收集稿  
   - [R-002 事实准入与 AI 协作](attachments/r-002-fact-admission-ai-collaboration.md)（`pending-user-review`）  
   - [R-003 工作区与共享资料](attachments/r-003-workspace-shared-materials.md)（`pending-user-review`）  
   二者均不关闭 F-002/F-003/F-004，不将相关 I-00N 标为 `verified`。
5. **硬禁令（重申，与 D-009 一致且继续有效）**：  
   - F-005 仍 open → **不**创建实现子目标；  
   - F-007/F-008 仍 open **或** I-003/I-004/I-006 未 `verified` → **不**开放 Web/AI 写入；  
   - 不创建生产 `ops/receipts/`，不把规格/收集稿当作运行证据。
6. 本轮不调整 GOAL-009 的 `status`/`progress`（仍为 `active / 0%`），无 residual risk 接受。

**为什么**：A-016 已把规格包与并行排期落到文档；本轮用户要求完成审视结论与 R-002/R-003 起草，避免 R-004 深度继续替代旅程与工作区/事实门禁。F-001 有完整边界证据可关；F-005 若强行关闭会掩盖 I-009 未决风险。

**未选方案**：

- 规格“有条件拒绝”并阻塞一切并行收集：小修订已足够，无需推倒重来。  
- 同时关闭 F-005：证据不足（三页/导航例外）。  
- 将 I-001/I-011 标为 `verified`：超出本轮可核对范围，违反 P-005。  
- 借规格接受立项实现或开放写入：违反硬禁令与 F-007/F-008。

**影响与后续**：A-017 记录响应与 finding 状态；`02-execution` 记录产物；`00-meta` 更新 I-001/I-011 证据与 R-002/R-003 索引。下一步：用户审视 R-002/R-003；可选裁决 F-005 关闭路径 α/β；可选 `/audit` 复审 F-001 关闭证据。

## D-011 · 产品打包与 dogfood 分栏，并确认导航/存储/F-005 与 SQLite 立场（2026-07-21）

**状态**：accepted

**确认来源**：用户澄清并要求落盘：Web 常作独立服务；Skills 加载到其他项目；核心方法论与模板随 Web/Skills 打副本；**本仓工作区、目标树、共享资料等过程记录不复制给其他项目、不为其他项目知悉**；其他项目自有工作区与共享资料。用户同时要求在落盘后审视该澄清对导航/存储建议的影响，并评估 Web 第一阶段是否宜直接提供 SQLite 以存结构性数据。

**决定**：

### A. 产品打包与数据分栏（用户澄清 · 权威边界）

1. **可分发产品（发布物）**：Web 应用、Skills 安装包/适配器、核心方法论与文档协议、canonical 模板及 contracts 镜像。上述可随产品打包**副本**，因其为服务所必需。
2. **不得进入发布物 / 不得成为其他项目默认上下文**：本开源/研发仓库内的过程性工作区（当前如 `docs/workspace-001-goal-governance/`）、其 `goal-tree.md` 与 `GOAL-*` 五件套内容、本仓共享资料正文与过程性审计附件、以及任何仅属于本仓 dogfood 的运行时状态。其他安装/其他 git 项目**不得**因安装 Web/Skills 而自动获知或加载上述内容。
3. **消费方运行时**：每个被服务的项目（或每次部署的数据根）拥有**自己的**工作区与（可选）共享资料区。Skills 只解析宿主项目配置/约定下的工作区；Web 只绑定部署配置的数据根与工作区列表。无配置时 fail closed，**禁止**静默回落到开发者 monorepo 的 dogfood 路径（本地开发可用显式 `DEV_DOGFOOD` 或等价开关，默认关闭）。
4. **术语**：  
   - **产品工作区（runtime）** = 消费方/部署内的目标真相源；  
   - **本仓治理工作区（dogfood）** = 仅本仓库过程记录。  
   二者协议相同、数据集不同；设计与实现不得混用「当前项目」一词掩盖差异。
5. **共享资料「跨工作区可见」**：仅指**同一部署/同一产品实例**内、该实例配置的共享资料根对其实例内多个工作区可见；**不是**跨安装、跨客户项目的全局资料云，也不是本仓 `docs/shared-materials/` 对外部项目可见。
6. **契约测试与示例**：使用合成 fixture 工作区；不得把本仓真实 GOAL-001～011 过程树打进发布测试包当作客户数据。

本条限定/澄清 D-001「canonical 根」与路径示例：原则是「**被打开的那份**工作区 canonical 根 + goal-tree」；本仓路径仅 dogfood 与文档示例。

### B. 澄清后对导航 / 存储 / F-005 建议的再审视结论（接受为设计默认）

澄清**不推翻**先前行业实践推荐，但**收紧主语**为「部署/消费方」：

| 主题 | 设计默认 | 澄清后的精确含义 |
|------|----------|------------------|
| 导航 | **N1 最小导航** | 列表仅含实例配置范围内工作区的 `workspace_id`、显示名、`root_goal` id、可选 active/archived；**不**扫描或暴露本仓 dogfood；进入详情后从该工作区 canonical 重载 |
| 共享资料字节存储 | **方案 A（消费方/部署旁路目录）** | 资料落在**该部署数据根**旁路目录（布局可类比 `shared-materials/`）；**不是**从本开源仓复制过程资料。大文件/密钥触发条件满足后再评估外置根（原 B）；对象存储（原 C）延后 |
| F-005 关闭 | **路径 α** | 书面收窄：首个实现子目标 = 单一**配置选中的产品工作区**详情（目标树核心）+ 用户提供执行事实的受限追加路径；总览/列表/N1 完整产品化与共享资料 CRUD 不在首实现目标内，另立门禁收集 |

N0 过严（单用户本机产品受损）；N2 首期不取（易成第二状态源）。路径 β（先审完三页再立项）不作为默认，可与 α 并行收集但不挡 α。

**F-005 状态**：本决定**接受路径 α 为关闭条件**；在将 α 的范围句写入成功标准/R-001 并完成一次可核对核对后，可由后续 `/govern` 关闭 F-005。**本条本身不关闭 F-005**（避免未改成功标准即关门）。关闭前仍禁止创建实现子目标。

### C. SQLite 与结构性数据（相对 D-005 的限定）

1. **Canonical 目标生命周期状态**（五件套、`goal-tree.md`、status/progress/parent、审计意见正文）**禁止**以 SQLite（或任何 DB）为权威源；Markdown 工作区根仍是唯一真相。
2. **D-006 首垂直切片 / R-004 写入路径**：保持 **无 SQLite 依赖**；receipt 仍按 D-007 为工作区旁路 `ops/receipts`（或等价非 canonical 文件）。不因「第一阶段 Web」而把写入契约绑到 DB。
3. **Web 产品在更广的第一阶段（含多工作区 N1、资料目录、可重建索引）**：允许引入 **SQLite 仅作结构性、非 canonical 存储**，且必须同时满足：  
   - 库文件位于**该部署数据根**内，不进发布物、不含本仓 dogfood；  
   - 内容限于：工作区导航注册/缓存、共享资料元数据索引、ops/receipt 查询辅助、UI/会话类结构等；  
   - **可自文件系统/canonical 重建**或明确「丢失则 fail closed / 降级扫描」，不得成为唯一不可恢复权威；  
   - 与 canonical 冲突时以 Markdown 为准；禁止把 finding 关闭、status、执行事实只写在 SQLite。  
4. **是否「第一阶段就上 SQLite」的结论**：**不是首切片的优选必选项**；在仅做详情 + 受限追加时，文件系统足够且更少双写风险。当实现范围扩展到 N1 列表/资料目录且扫描成本或查询需求明确时，再在实现目标中引入 SQLite 索引层。此结论**部分限定** D-005「当前收集与首切片不引入数据库」：首切片仍不引入；产品结构性索引层允许在后续实现目标中选型 SQLite，不再等同于「永远不做 DB」。
5. I-005 / I-009 继续收集：SQLite 文件权限、备份、迁移与「索引脏读」的 fail-closed 行为；未 verified 前不开放依赖 DB 的写入验收。

### D. 硬禁令与状态

- F-005 在按 α 正式关闭前：**不**创建实现子目标。  
- F-007/F-008 关闭且 I-003/I-004/I-006 `verified` 前：**不**开放 Web/AI 写入。  
- 本轮不改 GOAL-009 `status`/`progress`，无 residual risk 接受，不修改 `web/` 代码。

**为什么**：用户澄清是打包与隐私边界的权威事实，必须与多工作区产品模型分栏，否则导航/资料/DB 选型会把 dogfood 误当成产品数据。N1/A/α 在「部署级主语」下仍最符合单用户本地优先与垂直切片实践；SQLite 适合可重建索引，不适合首切片真相写入路径。

**未选方案**：

- 发布物附带示例「真过程树」：他项目会误载本仓上下文。  
- N0 或把本仓路径写死为唯一数据根。  
- 首切片用 SQLite 存执行事实或 goal status：第二真相源 + 违背 D-007/R-004。  
- 立即在本目标实现 SQLite：F-005/F-007/F-008 仍 open。

**影响与后续**：修订 R-003/R-001 边界与默认方案；更新 I-005/I-009/I-010 证据栏；A-018 记录响应。下一步：按 α 收窄 F-005 关闭材料（若用户确认立即关 F-005 可再开一轮）；用户继续审视 R-002；实现仍不得开工直至 F-005 关闭且写入门禁满足。

## D-012 · 接受建议选项、关闭 F-005（α）并立项 GOAL-012（2026-07-21）

**状态**：accepted

**确认来源**：用户明确「接受建议选项，开始推进」。

**决定**：

1. **全盘接受**此前建议并已由 D-011 记录的设计默认，作为推进基线：  
   - 导航 **N1**；共享资料字节存储 **A（部署旁路）**；F-005 **路径 α**；  
   - 打包/dogfood 分栏（D-011-A）；  
   - SQLite：**非**首切片依赖；后续仅可作非 canonical 可重建索引；  
   - R-002 设计默认（来源枚举不拆顶层、ai-knowledge 可确认入库但须标签、敏感工具每次同意、计算视图禁止一键变事实）。R-002 附件仍可迭代细节，不阻塞 α 实现立项。
2. **关闭 F-005**（A-001 required）：关闭证据为 D-006/D-011/D-012 + R-001 §5/§7.2/§8 与成功标准 α 收窄句——首个实现子目标范围 = 配置选中的单一产品工作区详情（目标树核心）+ 用户提供执行事实的受限追加路径；总览/列表/N1 产品化、共享资料 CRUD、AI 不在该实现目标内。目标树为核心已在 R-001/D-006 固定。
3. **授权创建** [GOAL-012-first-slice-workspace-detail](../GOAL-012-first-slice-workspace-detail/00-meta.md) 作为路线图 D 的实现子目标（`parent: GOAL-001-main-vision`，规划来源 GOAL-009）。
4. **不**因本条关闭 F-007/F-008，**不**将 I-003/I-004/I-006 标为 `verified`，**不**开放生产 Web/AI 写入；GOAL-012 必须在门禁内实施（fixture/测试可授权，生产路径保持关闭直至门禁满足）。
5. **不**关闭 F-002/F-003/F-004；路线图 B 相关收集继续，但不阻塞 α 实现目标的只读与契约测试工作。
6. 更新本目标成功标准与路线图：阶段 A 对「首实现立项条件」视为满足；阶段 D 转为由 GOAL-012 执行。GOAL-009 保持 `active`（规划与门禁台账仍开放），进度调至反映规划可交付实现立项（见 meta）。

**为什么**：用户确认推进；F-005 的 α 证据已齐；继续只规划不立项会空转。写入门禁必须留在 GOAL-012 与 F-007/F-008 上，避免「立项=放行写入」。

**未选方案**：

- 关闭 F-005 却不立项：与「开始推进」不符。  
- 立项同时开放生产写入：违反 F-007/F-008。  
- 把 N1/资料/AI 塞进 GOAL-012：违背 α。

**影响与后续**：A-019 关闭 F-005；创建 GOAL-012 五件套；更新 goal-tree。下一拍在 GOAL-012：配置 schema + fixture + 只读详情绑定。

## D-013 · 关闭 F-007（确认/信任边界）；F-008 仍 open 时写明范围（2026-07-21）

**状态**：accepted

**确认来源**：用户 `/govern 关闭 GOAL-009 F-007（证据已齐；F-008 仍 open 时写明范围`。

**决定**：

1. **正式关闭 F-007**（A-002 required）：关闭 scope = α 单用户受控写入的**确认与信任边界**，以 R-004/规格 + 可运行 CT 为证据，不扩展到 F-008 的并发/恢复原子性与完整审计 linkage。
2. **关闭证据集**（对照 A-022 关闭条件 1–4 + 设计裁决）：
   - 设计：D-007 / R-004 / 规格包（candidate→proposal→decide_and_execute、baseline、operation_id、trust_context）。
   - CT-001 missing/digest 绑定；CT-003 跨 workspace / path escape；CT-006 过期/reject/cancel/unknown；CT-012/014 内容与治理契约；CT-015 外部/非本地 trust 拒绝；CT-013 产品门禁默认拒绝写入。
   - 实现与回归：`web/services/controlled_change.py`；GOAL-013 阶段 C/E；web unittest **61 passed, 1 skipped**。
3. **F-008 明确仍 open**（本条不关闭）：范围含跨进程/跨部署协调（CT-009 现仅 process-local）、完整 audit linkage / 操作原子一致性（CT-011 现为最小 receipt 可核对）、以及任何「确认的 diff 即最终写入 diff」在多进程/恢复场景下的完整证明。关闭 F-008 须另一次用户裁决（接受 residual 或补证据）。
4. **生产写入继续阻断**：关闭 F-007 **不等于** 设置 `PRODUCT_GATES_OPEN=false`，**不等于** I-003/I-004/I-006 `verified`。生产放行仍要求 **F-008 closed** 且 I-003/I-004/I-006 `verified`（见 README 检查清单）。
5. **I-003**：本条仅关闭 F-007 对应的确认/信任 required finding；I-003 整项保持 `collecting`（产品 UX 与全矩阵验收未标 verified）。

**为什么**：A-022/A-024 已确认 F-007 运行证据齐备，仅缺台账正式关闭；用户明确授权分批关闭，且要求在 F-008 仍 open 时写明范围，避免「关一开全」。

**未选方案**：

- 同时关闭 F-008：证据边界与 residual 未裁决。  
- 关闭 F-007 并开放生产写入：违反硬禁令。  
- 将 I-003 标 `verified`：超出本 finding 关闭范围。

**影响**：A-025 台账关闭 F-007；更新 meta/execution/goal-tree/README 表述；下一拍建议审视 F-008 residual 或推进 F-002～F-004。

## D-014 · F-008 residual 路径包（proposed · 待用户裁决）（2026-07-21）

**状态**：superseded by D-014-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视 F-008 residual（接受 process-local + 最小 CT-011，或补跨进程协调` → 形成选项包。

**拟议选项**（全文见 [A-026](03-audit.md#a-026--f-008-residual-审视路径-ab待用户裁决2026-07-21)）：路径 A / B / C（历史选项表保留于 A-026）。

## D-014-A · 接受 R-F008-1～3 residual 并有界关闭 F-008（2026-07-21）

**状态**：accepted

**确认来源**：用户在 A-026 审视后明确选择 **路径 A**（接受 R-F008-1～3 residual，有界关闭 F-008）。

**决定**：

1. **有界关闭 F-008**：关闭 scope = **α / 单进程 / local-loopback-single-user** 部署假设下的受控写入操作身份一致性（幂等、进程内串行、recovery 阻断、最小 receipt 可核对）。
2. **接受下列 residual**（P-005 `accepted-residual`；责任人=用户；不假装已验证）：

| ID | 残余范围 | 复审触发（任一则 residual 须重新审视） |
|----|----------|----------------------------------------|
| **R-F008-1** | CT-009 仅 process-local lock；非跨进程/跨主机互斥 | 多 worker / 多实例 / 共享 FS 并发写部署前 |
| **R-F008-2** | CT-011 最小 receipt 可核对；非完整确认—审计—canonical 三方 linkage | 生产写入验收前；或引入完整 audit linkage 对象后 |
| **R-F008-3** | 多进程崩溃/半写时序未全谱演练 | 试点恢复演练；多进程部署前 |

3. **不**因本条：设置 `PRODUCT_GATES_OPEN=false`；将 I-003/I-004/I-006 标 `verified`；开放生产 Web/AI 写入。  
4. **不**立项 GOAL-014（路径 B/C 未选）；消除 residual 可在复审触发时再立目标。  
5. 生产写入检查清单：F-007 **与** F-008 现均为 closed（F-008 有界 + residual）；**仍须** I-003/I-004/I-006 `verified` 与显式 env 后才可启用。

**为什么**：与 D-007 trust_context 与 α 切片一致；运行证据已覆盖有界 scope；跨进程为部署形态变更，宜用 residual 复审触发而非阻塞当前台账。

**未选方案**：路径 B（立即跨进程实现）；路径 C（并行立项）；无 residual 宣称 F-008 全量关闭。

**影响**：A-027 关闭 F-008；更新 meta/execution/goal-tree/README；生产写入仍阻断。

## D-015 · I-003/I-004/I-006 verified 路径包（proposed · 待用户裁决）（2026-07-21）

**状态**：superseded by D-015-V1（用户已选 V1）

**确认来源**：用户 `/govern 审视 I-003/I-004/I-006 可否 verified` → [A-028](03-audit.md#a-028--审视-i-003--i-004--i-006-可否-verified2026-07-21)。

## D-015-V1 · α 有界将 I-003/I-004/I-006 标 verified（2026-07-21）

**状态**：accepted

**确认来源**：用户在 A-028 后明确选择 **V1**：三项均按 α 有界 verified（I-006=写入 fail-closed 子集）。

**决定**：

1. **I-003 → verified（α）**  
   - Scope：单用户、无多角色；`append-execution-fact` 提案/预览/affirm|reject|cancel；service/API + CT 负向（含 CT-001/002/003/006/012/014/015/016/018）。  
   - 非 scope：完整浏览器 UX E2E 矩阵；Web「发起审计」产品路径。

2. **I-004 → verified（α + residual）**  
   - Scope：写集仅 `02-execution.md`；基线/digest；operation id 幂等；进程内并发；recovery_pending；最小 receipt 可核对；工作区绑定。  
   - 挂接 **R-F008-1～3**（D-014-A）。  
   - 非 scope：**AI 提案溯源**（归后续 AI 接入 / I-002·I-008）；跨进程全文互斥（residual）。

3. **I-006 → verified（受控写入 fail-closed 子集）**  
   - Scope：未确认/非法内容拒写、跨 workspace 写入拒绝、open finding 下受限追加、recovery 阻断、产品门禁默认拒写、trust 拒绝。  
   - 非 scope：AI 不可用/输出不合法、Web 审计冲突升级 UX、试点级全矩阵与可用性（可在路线图 D/E 再收集；不撤销本 α verified，除非部署形态变化触发 residual 复审）。

4. **不**设置 `PRODUCT_GATES_OPEN=false`；**不**设置 `ALLOW_CONTROLLED_WRITE=true`；生产写入仍须用户显式 env + 单进程 residual 范围。

**为什么**：与 F-007/F-008 有界关闭一致；运行证据 61 passed；避免用 α 证据伪装全文产品/AI 已验证。

**未选**：V2（I-006 继续阻断）、V3（全不 verified）、V4（全文无证据）。

**影响**：A-029 台账；meta 三行状态；README 清单更新；进度上调；生产写入**仍默认关**。

## D-016 · 放行生产受控写入（设 env / 规划锁默认关闭）（2026-07-21）

**状态**：accepted

**确认来源**：用户 `/govern 放行生产受控写入（设 env`。

**前提（已满足）**：F-007/F-008 closed；I-003/I-004/I-006 verified（α）；R-F008-1～3 residual accepted（D-014-A）。

**决定**：

1. **授权**在下列条件下启用生产路径 `decide_and_execute`：  
   - `GOAL_GOVERNANCE_PRODUCT_GATES_OPEN` 为 false（规划锁关闭）  
   - `GOAL_GOVERNANCE_ALLOW_CONTROLLED_WRITE=true`（第二门闩显式打开）  
   - 工作区为**产品数据根**（`WORKSPACE_DIR` / `DATA_ROOT`），**禁止**与 `DEV_DOGFOOD=true` 同开  
   - 部署为 **单进程** local-loopback（或 residual 已复审）  
2. **代码默认**：`production_product_gates_open` 默认由 `true` 改为 **`false`**（A-030 起规划锁默认关闭）。  
3. **第二门闩默认不变**：`ALLOW_CONTROLLED_WRITE` 默认仍为 **`false`** — 部署须显式设 true 才可写；避免空 env 误开写。  
4. 更新 `web/.env.example` 与 README 检查清单 / 推荐 env 片段。  
5. 设 `PRODUCT_GATES_OPEN=true` 可作为紧急再阻断，无需回退代码。

**为什么**：台账门禁已齐；双门闩保留操作显式性；默认关闭规划锁反映 A-029 事实，ALLOW 防止裸进程误写。

**未选方案**：将 ALLOW 默认 true（过宽）；仅改文档不改代码默认（与「设 env / 放行」及台账闭环不一致）；对 dogfood 过程树开放生产写。

**影响**：A-030；测试与 README 同步；进度上调；生产写在 **ALLOW=true + 产品数据根** 时可用。

## D-017 · 冻结 R-002 验证包（不关闭 F-002）（2026-07-21）

**状态**：accepted

**确认来源**：用户 `/govern` 确认 **「OK 推进 F-002（R-002 验证包）」**。

**决定**：

1. 接受 [r-002-verification-package.md](attachments/r-002-verification-package.md) 为路线图 B **事实准入设计冻结 + FA 负向验证计划**的权威基线（version 0.1.0）。  
2. 将 D-012 设计默认冻结为可测规范，并明确四项裁决：  
   - `source_kind` 保持五类；用户上传归 `user-provided` + 可选 `source_refs`，不拆枚举；  
   - `ai-knowledge` 可进 canonical，必须保留标签与 `source_kind`；  
   - 工具同意：**每次敏感调用**；只读本工作区 canonical 复述可免；  
   - ComputedView **禁止**一键变事实。  
3. **F-002 保持 open / required**，直至核心 **FA-001～FA-006** 有可核对 pass 证据（或用户另令接受有界关闭 + residual R-F002-1～3）。  
4. **不**将 I-002、I-008 标 `verified`；**不**授权 AI 运行时接入；**不**扩展 α 写动作集（仍仅 `user-provided` + `append-execution-fact`）。  
5. 更新 R-002 收集稿索引与 GOAL-009 信息表证据栏；A-031 记录本拍响应。

**为什么**：F-002 关闭要求同时需要「契约」与「负向验证」。契约与计划可先冻结；未跑 FA 不得假装 finding 已关。

**未选方案**：

- 仅因设计默认 accepted 就关闭 F-002：违反 P-005 / finding 关闭证据规则。  
- 本拍直接实现 AI broker：超出「验证包」确认范围，且 I-002 未 verified。  
- 本拍无用户裁决即采用 F-002 有界 residual 关闭：路径 A 保留在验证包内，须另令。

**影响与后续**：路线图 B 在 F-002 子范围进入「计划已冻结、执行未跑」；下一步实现/执行 FA-001～006 后再审视关闭。F-003/F-004 仍 open，本拍不处理。

## D-018 · F-002 关闭路径包（proposed · 待用户裁决）（2026-07-21）

**状态**：superseded by D-018-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视关闭 F-002` → [A-033](03-audit.md#a-033--审视关闭-f-002路径a--待裁决2026-07-21)。

## D-018-A · 接受 R-F002-1～3 residual 并有界关闭 F-002（2026-07-21）

**状态**：accepted

**确认来源**：用户在 A-033 后明确选择 **路径 A**：`OK A：F-002 有界关闭 + R-F002-1～3`。

**决定**：

1. **有界关闭 F-002**：关闭 scope = 事实准入的**数据与交互契约**（D-017 验证包）+ 核心负向 **FA-001～FA-006** 纯函数/服务层 pass（无来源、来源伪装、未确认混入 view-model、来源变更/digest、同 revision 静默改 kind）。  
2. **接受下列 residual**（P-005 `accepted-residual`；责任人=用户；不假装已验证）：

| ID | 残余范围 | 复审触发（任一则 residual 须重新审视） |
|----|----------|----------------------------------------|
| **R-F002-1** | FA-004 仅 view-model 契约；非浏览器 DOM/E2E 全量渲染 | 候选面板 UI 合并前；或宣称 UI 验收前 |
| **R-F002-2** | FA-007～012 AI/工具运行时未执行 | AI 接入立项或首次工具调用实现前 |
| **R-F002-3** | I-002 提供方/模型/加载未 verified | AI 技术方案冻结前 |

3. **不**因本条：将 I-002 或 I-008 标 `verified`；开放 AI 写入/工具路径；退出路线图 B（**F-003/F-004 仍 open**）；修改生产 env 门闩。  
4. α 写动作集不变：仍仅 `user-provided` + `append-execution-fact`（R-004）。  
5. 关闭声明写入 [A-034](03-audit.md#a-034--路径-a有界关闭-f-002并接受-r-f002-132026-07-21)。

**为什么**：与 F-008 / I-003 α 有界关闭一致；A-001 关闭要求在契约 + 核心负向层已可核对；UI/AI 后置用 residual 复审触发，避免阻塞 F-002 台账或伪装全文验证。

**未选方案**：路径 B（等扩展 FA/UI）；路径 C（不关 finding）；无 residual 全文关闭（证据不足）。

**影响**：A-034 台账关闭 F-002；更新 meta/execution/goal-tree/验证包；进度上调；路线图 B 仍因 F-003/F-004 进行中。

## D-019 · 冻结 R-003 验证包（不关闭 F-003/F-004）（2026-07-21）

**状态**：accepted

**确认来源**：用户 `/govern 推进 F-003～F-004（R-003 验证包`。

**决定**：

1. 接受 [r-003-verification-package.md](attachments/r-003-verification-package.md) 为路线图 B **工作区隔离 + 共享资料**的设计冻结与 WS/SM 负向验证计划权威基线（version 0.1.0）。  
2. 将 D-011 / D-004 设计默认冻结为可测规范，明确：N1 导航、存储 A、打包/dogfood 分栏、SQLite 非 canonical、共享资料用户 CRUD + 固定引用、α 单工作区边界。  
3. **F-003 保持 open**，直至核心 **WS-001～WS-006** pass 或用户另令有界关闭 + residual。  
4. **F-004 保持 open**，直至核心 **SM-001～SM-006** pass 或用户另令有界关闭 + residual。  
5. **不**将 I-009、I-010 标 `verified`；**不**自动立项资料 CRUD 实现目标；**不**改生产 env；**不**接入 AI。  
6. F-004 关闭证据以 **D-004/A-007 共享资料语义**为准，不得回退到已废弃的「只读贡献资料」要求。  
7. A-035 记录本拍响应。

**为什么**：F-003/F-004 关闭要求同时需要契约与负向验证；契约与计划可先冻结，未跑 WS/SM 不得假装 finding 已关（对齐 R-002 / D-017 节奏）。

**未选方案**：仅因 D-011/GOAL-010/011 输入即关 F-003/F-004；本拍实现完整资料 UI；无用户裁决即有界 residual 关闭。

**影响与后续**：路线图 B 的 F-003/F-004 子范围进入「计划已冻结、执行未跑」；下一步实现/执行 WS 与/或 SM 矩阵后再审视关闭。

## D-020 · F-003/F-004 关闭路径包（proposed · 待用户裁决）（2026-07-21）

**状态**：superseded by D-020-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视关闭 F-003 与 F-004` → [A-037](03-audit.md#a-037--审视关闭-f-003与-f-004路径a--待裁决2026-07-21)。

## D-020-A · 接受 R-F003/R-F004 residual 并有界关闭 F-003 与 F-004（2026-07-21）

**状态**：accepted

**确认来源**：用户在 A-037 后明确选择 **路径 A**：`OK A：F-003 与 F-004 有界关闭 + R-F003-1～2 / R-F004-1～3`。

**决定**：

1. **有界关闭 F-003**：关闭 scope = 工作区 → Root/canonical 映射、N1 索引边界、跨区访问拒绝与 dogfood 默认拒绝的**契约 + WS-001～006 纯函数/服务层**证据（D-019 / A-036）。  
2. **有界关闭 F-004**：关闭 scope = 共享资料固定引用、哈希、工作区匹配、删除前引用检查、AI 不执行/不外传、资料 API 不碰 goal 路径的**契约 + SM-001～006 纯函数/服务层**证据（D-004/D-019 / A-036）。  
3. **接受 residual**（P-005 `accepted-residual`；责任人=用户）：

| ID | 残余范围 | 复审触发 |
|----|----------|----------|
| **R-F003-1** | 多工作区产品 UI / 列表·创建·归档 E2E（WS-008） | 平台导航/多区产品立项或 UI 合并前 |
| **R-F003-2** | 发布物无 dogfood 过程树验收（WS-007） | 正式发布验收前 |
| **R-F004-1** | 全量用户 CRUD E2E + 历史保留（SM-007） | 资料区产品实现验收前 |
| **R-F004-2** | AI 读运行时与提示注入实机路径 | AI 读共享资料接入前 |
| **R-F004-3** | 发布/安装资料边界与索引-FS 冲突扩展（SM-008/010） | 含资料的发布或索引实现前 |

4. **不**因本条：将 I-009/I-010 标 `verified`；自动立项或交付资料 CRUD 产品；修改生产 env；撤销 α 写动作边界。  
5. 关闭声明写入 [A-038](03-audit.md#a-038--路径-a有界关闭-f-003与-f-0042026-07-21)。  
6. 路线图 B 在 **required finding** 侧视为**有界退出**（F-002～F-004 均已有界 closed）；I-002 等 AI 接入信息项仍可继续收集。

**为什么**：与 F-002 路径 A 一致；核心契约与 WS/SM 负向已可核对；产品 UI/CRUD/发布验收用 residual 复审触发，避免伪装全文交付。

**未选**：B/C（只关其一）、D（不关）、E（无 residual 全文关）。

**影响**：A-038 台账；meta 成功标准/路线图 B；progress 上调；goal-tree 同步。

## D-021 · 响应 A-039：关闭 F-025、登记 F-026/F-027，并定 E-α 入口（2026-07-22）

**状态**：accepted

**确认来源**：用户 `/govern 响应 A-039：先改 F-025（00-meta R-004 索引 + 路线图 A 行），再登记 F-026/F-027；然后定 E 试点或 residual`。

**P-004**：A-039 为 `source: independent`，无同 scope self。用户本轮书面指令直接响应并写入，**不**另开同范围 self（与 A-039 建议一致）。本条为编排响应，**不**伪装为 independent。

**决定**：

### 1. 关闭 F-025（required）

1. 修订 [00-meta.md](00-meta.md)：  
   - **路线图 A**：改为「完成（α 立项条件）」；删除「F-003/F-004 与路线图 B 仍开放」过期句。  
   - **R-004 索引**：删除「CT-001～CT-018；未执行」；改为历史计划 + 执行证据索引（A-020～A-030 / GOAL-013 / `web/tests`）。  
2. 在 [r-004-contract-test-plan.md](attachments/r-004-contract-test-plan.md) 文首增加 **执行状态索引**，避免历史「待执行」正文被读成当前事实。  
3. **F-025 → closed**（证据：本决定 + meta/附件 diff + A-040）。

### 2. 登记 F-026 / F-027（recommended · 保持 open）

| Finding | 登记动作 | 状态 |
|---------|----------|------|
| **F-026** | 台账：FA/WS/SM 未挂 `main`/`controlled_change` 热路径；α 仅靠平行门禁（user-provided + workspace_config）。扩多来源/多区/资料 API 前须组合或显式 residual 复审。 | **open / recommended** |
| **F-027** | 将 I-002/I-008/I-009/I-010 的「最晚需要阶段」从「路线图 B 结束前」改为挂接 AI 接入 / UI·多区 / 资料产品等后续门禁；写明 B finding 有界退出不解除 I 门禁。 | **closed as documentation alignment**（信息表已改；finding 本身 recommended 可标 closed，因关闭要求=对齐字段） |

说明：F-027 的关闭要求是「对齐 I 最晚阶段字段」；本拍在 meta 信息表完成对齐后，A-040 将 **F-027 closed**。F-026 仍 open。

### 3. 定路线图 E 入口：选 **E-α（试点冒烟）**，不选 residual-only

| 路径 | 含义 | 本拍 |
|------|------|------|
| **E-α（选用）** | 进入路线图 E 的 **α 有界冒烟**：产品数据根 + `ALLOW_CONTROLLED_WRITE=true` + 单进程；一次 user-provided 受限追加 + `/api/health` + 可选负向抽检；**不**扩 AI/资料 CRUD/多区 UI | **已定入口**；冒烟**本拍不执行**（须用户下一句显式跑） |
| residual-only | 仅维护 residual 表、不启 E | **未选** |
| E 全文退出 | 真实使用证据 + 全矩阵 + 阶段审视关门 | **未选**；冒烟成功 ≠ E 退出 |

**E-α 范围（下一拍执行时）**：

1. 数据根：`data/product-workspace` 或等价产品根（**禁止** dogfood 过程树生产写）。  
2. env：`PRODUCT_GATES_OPEN=false`、`ALLOW_CONTROLLED_WRITE=true`、`DEV_DOGFOOD=false`。  
3. 动作：对 fixture 目标追加一条可识别的 user-provided 执行事实；核对 receipt 与 `02-execution`。  
4. 不关闭 residual R-F002/F003/F004/F008；不将 I-002/I-008/I-009/I-010 标 verified。  
5. F-026：E-α 可在 α 平行门禁下进行；**不**因 E-α 要求先组合 FA/WS/SM 热路径。

**为什么**：F-025 阻断的是索引可信度，修正后即可进入有界试点准备；A-039 确认有界 finding 证据充分。residual-only 会空转；E 全文尚无试点证据。

**未选方案**：

- 因 F-026 回退 F-002～F-004 closed：与 A-039 独立复审及有界 scope 不符。  
- 本拍静默执行生产写入冒烟：用户仅定路径，未授权本轮写 canonical/产品数据。  
- residual-only 搁置 E：与「定 E 试点或 residual」中推进意图不符。

**影响与后续**：A-040 台账；meta progress **82%**；goal-tree 同步。下一句建议：`/govern 执行 E-α 产品工作区受控追加冒烟`。

## D-022 · 执行 E-α 产品工作区受控追加冒烟（2026-07-22）

**状态**：accepted

**确认来源**：用户 `/govern 执行 E-α 产品工作区受控追加冒烟`（D-021 入口）。

**决定**：

1. **授权并执行** D-021 定义的 E-α 冒烟（本拍已发生，非仅计划）：  
   - 数据根 `data/product-workspace`；**禁止** dogfood 过程树。  
   - 生产路径：`test_authorized=False` + `ALLOW_CONTROLLED_WRITE=true` + `PRODUCT_GATES_OPEN=false` + `DEV_DOGFOOD=false`。  
   - 对 `GOAL-001-fixture-target` 追加一条可识别 user-provided 执行事实；operation_id=`op_e_alpha_smoke_20260722`。  
2. **记录证据**：[attachments/e-alpha-smoke-2026-07-22.md](attachments/e-alpha-smoke-2026-07-22.md) + A-041。  
3. **路线图 E**：状态更新为 **E-α 冒烟已完成**；**不**宣称 E 全文退出或 GOAL-009 关门。  
4. **不**关闭 residual；**不** verified I-002/I-008/I-009/I-010；**不**关闭 F-026。  

**结果（事实）**：`decide_and_execute` → **committed**；meta/tree 不变；receipt 落盘；门禁负向抽检 pass；事后 unittest **80 passed, 1 skipped**。

**为什么**：用户明确授权执行；门禁与产品根条件已满足。

**未选**：浏览器 E2E 冒烟（本拍 service 生产路径足够）；对 dogfood 写；E 全文关门。

**影响**：progress **85%**；goal-tree 同步；下一拍可选 residual 复审、F-026 挂接、或更多试点使用。

## D-023 · 路线图 E 有界退出路径包（proposed · 已由 D-023-A 取代）（2026-07-22）

**状态**：superseded by D-023-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视路线图 E 是否有界退出` → [A-042](03-audit.md#a-042--审视路线图-e-是否有界退出路径a--待裁决2026-07-22)。

历史选项 A/B/C/D 见 A-042；生效裁决见下条 D-023-A。

## D-023-A · 接受 R-E-1～3 residual 并有界退出路线图 E（E-α）（2026-07-22）

**状态**：accepted

**确认来源**：用户在 A-042 后明确选择 **路径 A**：`OK A：E-α 有界退出 + R-E-1～3`。

**决定**：

1. **有界退出路线图 E**：退出 scope = **E-α** — α 产品工作区、生产受控写路径、单次 user-provided `append-execution-fact` 冒烟 + 门禁负向抽检 + A-041 审计结论（D-021/D-022）。  
2. **接受 residual**（P-005 `accepted-residual`；责任人=用户）：

| ID | 残余范围 | 复审触发（任一则 residual 须重新审视） |
|----|----------|----------------------------------------|
| **R-E-1** | HTTP/表单或浏览器 E2E 受控追加路径未跑 | UI 合并验收前；或宣称「页面写入已试点」前 |
| **R-E-2** | 多会话/持续真实人类使用；I-007/I-012 试点标准未收集 | 受控试点扩大或体验验收前 |
| **R-E-3** | E 全文：整改环、扩展产品（AI/资料/多区）齐套与 GOAL-009 阶段/关门审 | 宣称阶段 6 规划目标可关门或扩展产品放行前 |

3. **不**因本条：将 GOAL-009 标 `done`；将 I-002/I-008/I-009/I-010 标 `verified`；关闭 F-026；撤销 R-F002～F004 / R-F008；开放 AI/资料 CRUD/多区产品；改生产 env 默认值。  
4. 关闭声明写入 [A-043](03-audit.md#a-043--路径-a有界退出路线图-e-e-α并接受-r-e-132026-07-22)。  
5. 路线图 A～E 在 **α 规划/有界交付** 侧均已退出或完成；GOAL-009 仍 `active`（扩展与 residual 复审、可选关门审）。

**为什么**：与路线图 B / F-008 有界退出纪律一致；A-041 在 E-α 范围内可核对；全文条件用 residual 复审触发，避免伪装阶段 6 可关门。

**未选**：B（先 HTTP 再退）、C（不声明退出）、D（无 residual 全文退）。

**影响**：meta 路线图 E + residual 表；progress **90%**；goal-tree 同步；I-007/I-012 挂 R-E-2。

## D-024 · 推进 F-026：热路径挂接 FA / WS / SM（2026-07-22）

**状态**：accepted

**确认来源**：用户 `/govern 推进 F-026 热路径挂接`。

**决定**：

1. 在 `web/services/controlled_change.py` 的 prepare / build_proposal / decide_and_execute（affirm 前）组合：  
   - **FA**：`validate_confirm_or_proposal`（含来源伪装拒绝）  
   - **WS**：`validate_cross_workspace_access`（WS-003）  
   - **SM**：执行写不得落在 shared-materials 路径（`ERR_SM_GOAL_PATH_VIA_MATERIALS`）  
2. 新增 unittest 证明 import 组合 + 负向 + 正例仍 committed。  
3. **关闭 F-026**（recommended）；**不**关 R-F002-1；**不** verified I-002/I-008。  

**证据**：[attachments/f-026-hot-path-composition-2026-07-22.md](attachments/f-026-hot-path-composition-2026-07-22.md)；A-044。  
**回归**：**84 passed, 1 skipped**。

**为什么**：A-039 指出库测 ≠ 热路径；用户授权推进挂接。

**未选**：仅文档声明不改代码；把 UI E2E 塞进 F-026。

**影响**：progress **92%**；goal-tree 同步。

## D-025 · 执行 R-E-1 HTTP 受控追加冒烟并关闭 residual（2026-07-22）

**状态**：accepted

**确认来源**：用户 `/govern 执行 R-E-1 HTTP 受控追加冒烟`。

**决定**：

1. 执行 HTTP **表单**路径冒烟：`POST /goals/{id}/proposal` → `POST .../decide`（TestClient + 产品根 `data/product-workspace` + 生产 ALLOW 门禁）。  
2. 固化单测 `test_r_e1_http_form_commit_when_write_authorized`。  
3. **关闭 residual R-E-1**（scope = HTTP/表单受控追加；**不**宣称全浏览器 DOM E2E 或 R-E-2/R-E-3）。  
4. 证据：[attachments/r-e1-http-smoke-2026-07-22.md](attachments/r-e1-http-smoke-2026-07-22.md)；A-045。  

**结果**：产品根 **committed**（`op_ed694c6922dd`）；回归 **85 passed, 1 skipped**。

**未选**：仅 service 路径冒烟（已有 E-α）；Selenium 全浏览器矩阵（超出 R-E-1「HTTP/表单」充分条件）。

**影响**：progress **94%**；R-E-2 / R-E-3 仍 accepted open。

## D-026 · R-E-2 有界关闭路径包（proposed · 已由 D-026-A 取代）（2026-07-22）

**状态**：superseded by D-026-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视 R-E-2 是否可有界关闭` → [A-046](03-audit.md#a-046--审视-r-e-2-是否可有界关闭路径a--待裁决2026-07-22)。

历史选项 A/B/C/D 见 A-046；生效裁决见下条 D-026-A。

## D-026-A · 有界关闭 R-E-2 并接受 R-E-2-H（2026-07-22）

**状态**：accepted

**确认来源**：用户在 A-046 后明确选择 **路径 A**：`OK A：R-E-2 有界关闭 + R-E-2-H`。

**决定**：

1. **有界关闭 R-E-2**：关闭 scope = **α 双路径受控写入试点使用**  
   - service 生产路径：A-041 / `op_e_alpha_smoke_20260722` **committed**  
   - HTTP 表单路径：A-045 / `op_ed694c6922dd` **committed** + 单测  
2. **接受 residual R-E-2-H**（P-005 `accepted-residual`；责任人=用户）：

| ID | 残余范围 | 复审触发 |
|----|----------|----------|
| **R-E-2-H** | 多会话/持续真实人类使用；I-007/I-012 试点标准未收集 | 扩大试点、体验验收、或宣称「人类试点完成」前 |

3. **不**因本条：verified I-007/I-012；关 R-E-3；关 GOAL-009；宣称人类日常试点完成。  
4. 关闭声明写入 [A-047](03-audit.md#a-047--路径-a有界关闭-r-e-2并接受-r-e-2-h2026-07-22)。  
5. I-007/I-012 延期栏改挂 **R-E-2-H**。

**为什么**：双路径自动化试点已可核对；人类多会话与体验标准仍缺，拆 residual 避免伪装。

**未选**：B（先补人类清单）、C（保持 open）、D（全文关无 residual）。

**影响**：progress **95%**；goal-tree 同步。

## D-027 · R-E-3 有界关闭路径包（proposed · 已由 D-027-A 取代）（2026-07-22）

**状态**：superseded by D-027-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视 R-E-3 是否可有界关闭` → [A-048](03-audit.md#a-048--审视-r-e-3-是否可有界关闭路径a--待裁决2026-07-22)。

历史选项 A/B/C/D 见 A-048；生效裁决见下条 D-027-A。

## D-027-A · 有界关闭 R-E-3 并接受 R-E-3-X（2026-07-22）

**状态**：accepted

**确认来源**：用户在 A-048 后明确选择 **路径 A**：`OK A：R-E-3 有界关闭 + R-E-3-X`。

**决定**：

1. **有界关闭 R-E-3**：关闭 scope = **α E 阶段整改环在已声明 α 产品范围内完成**，证据包括：  
   - 路线图 A～E α/有界退出或完成  
   - R-E-1 closed；R-E-2 有界 closed；F-026 热路径；生产写双门闩  
   - A-041 service + A-045 HTTP 冒烟 **committed**  
2. **接受 residual R-E-3-X**（P-005 `accepted-residual`；责任人=用户）：

| ID | 残余范围 | 复审触发 |
|----|----------|----------|
| **R-E-3-X** | GOAL-009 正式阶段/关门审未做；AI 运行时/资料 CRUD/多区产品未齐套；成功标准 AI 项未勾；不得宣称阶段 6 Web 终态 | 拟将 GOAL-009 标 `done`、扩展产品放行、或宣称阶段 6 终态前 |

3. **不**因本条：GOAL-009 `status: done`；关闭 R-E-2-H；verified I-002/I-007/I-008/I-009/I-010/I-012；勾选 AI 成功标准。  
4. 关闭声明写入 [A-049](03-audit.md#a-049--路径-a有界关闭-r-e-3并接受-r-e-3-x2026-07-22)。  

**为什么**：α 范围内 E 整改/验证已可核对；关门与扩展必须另 residual，避免伪装阶段 6 可关门。

**未选**：B（先关门审）、C（保持 open）、D（全文关无 residual）。

**影响**：progress **96%**；goal-tree 同步；GOAL-009 仍 `active`。

## D-028 · 扩展立项选项包（proposed · 待用户选定）（2026-07-22）

**状态**：proposed（**未**创建 GOAL-014+；GOAL-009 **保持 active**）

**确认来源**：用户 `/govern 保持 GOAL-009 active，推进扩展立项选项`（承接 A-050）。

**决定（本拍仅选项冻结，不立项）**：

1. **GOAL-009 保持 `active`**；**不**关门；R-E-3-X 仍阻断 `done`。  
2. 将扩展工作整理为可独立交付的候选子目标（`parent: GOAL-001-main-vision`，规划来源 GOAL-009），按 P-001 **先选项后按阶段创建**，禁止一次堆砌全部实现。  
3. 候选与依赖见 [A-051](03-audit.md#a-051--扩展立项选项包goal-009-保持-active2026-07-22)；用户选定代号后再 `/govern 创建 GOAL-0xx…`。  

**推荐默认序**（可改）：

| 序 | 代号 | 建议 slug / 主题 | 主要门禁 |
|----|------|------------------|----------|
| 1 | **X-AI** | `ai-collaboration-runtime` · AI 协作运行时与确认链 | I-002；R-F002-2/3；成功标准 AI 项 |
| 2 | **X-NAV** | `n1-workspace-navigation` · N1 多工作区导航 | I-009；R-F003-1；I-011 列表部分 |
| 3 | **X-SM** | `shared-materials-product` · 共享资料 CRUD 产品 | I-010；R-F004-1～3 |
| 4 | **X-PILOT** | `human-pilot-feedback` · 人类多会话试点与 I-007/I-012 | R-E-2-H |
| 5 | **X-DEPLOY** | `deployment-ops-hardening` · 部署/密钥/备份 | I-005 |

**未选（本拍）**：立即创建任一 GOAL-014+；将 GOAL-009 标 done；静默合并多个扩展为一目标。

**影响**：仅台账；progress 可标 **97%** 不变或微调；goal-tree 注扩展选项待选。

## D-029 · 接受 R1：创建 GOAL-014（X-AI）（2026-07-22）

**状态**：accepted

**确认来源**：用户 `OK R1：创建 X-AI（GOAL-014 AI 协作运行时）`。

**决定**：

1. 创建 [GOAL-014-ai-collaboration-runtime](../GOAL-014-ai-collaboration-runtime/00-meta.md)，扩展代号 **X-AI**。  
2. GOAL-009 **保持 `active`**；**不**关闭 R-E-3-X；**不**勾选 AI 成功标准（由 GOAL-014 交付证据后再议）。  
3. 其余扩展代号（X-NAV / X-SM / X-PILOT / X-DEPLOY）仍保留在 D-028 选项表，**未**本拍创建。  

**为什么**：R1 优先补「AI 协助」愿景与 I-002 运行时缺口；α 写路径已由 GOAL-012/013 覆盖。

**影响**：goal-tree 新增 GOAL-014；A-052 记录响应。

## D-030 · AI 成功标准勾选路径包（proposed · 已由 D-030-A 取代）（2026-07-22）

**状态**：superseded by D-030-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视 GOAL-009 AI 成功标准是否可勾选` → [A-053](03-audit.md#a-053--审视-ai-成功标准是否可勾选路径a--待裁决2026-07-22)。

历史选项 A/B/C/D 见 A-053；生效裁决见下条 D-030-A。

## D-030-A · 有界勾选 GOAL-009 AI 成功标准（2026-07-22）

**状态**：accepted

**确认来源**：用户在 A-053 后明确选择 **路径 A**：`OK A：有界勾选 GOAL-009 AI 成功标准`。

**决定**：

1. 将初始成功标准第 3 条标为 **[x] 有界勾选**。  
2. 关闭证据：R-002 设计默认 + R-014-A 边界 + [GOAL-014](../GOAL-014-ai-collaboration-runtime/00-meta.md) 有界关门（职责/上下文/候选/确认边界/禁自动治理推进）。  
3. **I-002 整项保持 `collecting`**（提供方/模型全文、工具矩阵等仍可收集；R-F002-3 不因此关闭）。  
4. **R-F002-2**（工具运行时）、**R-014-E2E**（真联调/浏览器 E2E）保留。  
5. **不**因本条：GOAL-009 `done`；关闭 R-E-3-X；默认 `AI_ENABLED=true`；宣称检索工具已交付。  
6. progress **98%**。

**为什么**：成功标准主干为「定义 + 确认边界」；GOAL-014 有界交付已满足；括注信息项全文不得伪装 verified。

**未选**：B/C（不勾）；D（连 I-002 verified / 可关门）。

**影响**：00-meta 勾选句；A-054；goal-tree。

## D-031 · GOAL-009 有界关门路径包（proposed · 已由 D-031-A 取代）（2026-07-22）

**状态**：superseded by D-031-A（用户已选路径 A）

**确认来源**：用户 `/govern 审视 GOAL-009 是否可有界关门` → [A-055](03-audit.md#a-055--审视-goal-009-是否可有界关门路径a--待裁决2026-07-22)。

历史选项 A/B/C/D 见 A-055；生效裁决见下条 D-031-A。

## D-031-A · 有界关门 GOAL-009 并接受 R-009-X（2026-07-22）

**状态**：accepted

**确认来源**：用户在 A-055 后明确选择 **路径 A**：`OK A：GOAL-009 有界关门 + R-009-X`。

**决定**：

1. 执行有界 close-out（A-056）：关闭范围 = **规划台账 + α 实现闭环 + X-AI 有界定义/交付**。  
2. GOAL-009 → `status: done` · `progress: 100%`。  
3. **关闭 R-E-3-X**（关门审子句已由 A-055/A-056 完成）。  
4. **接受 residual R-009-X**（P-005）：

| ID | 残余范围 | 复审触发 |
|----|----------|----------|
| **R-009-X** | N1 多区产品、共享资料 CRUD 产品、人类多会话试点、I-002/I-008/I-009/I-010 全文 verified、阶段 6 产品终态宣称 | 宣称终态、扩展产品放行、或 Root 阶段 6 终态审前 |

5. **R-E-2-H** 并入 R-009-X（状态 closed-by-merge）。R-F* / R-014-E2E 保持各目标 residual。  
6. **不**因本条：Root GOAL-001 done；I-002 等标 verified；宣称一期 Web 终态；取消扩展选项表（X-NAV 等仍可另立目标）。  

**为什么**：规划职责与 α/X-AI 有界证据齐；扩展必须 residual，避免伪装终态。

**未选**：B/C（不关）；D（无 residual 全文关）。

**影响**：goal-tree；A-056。

## D-032 · 响应 A-057：关闭 F-029/F-030（现时叙述 + I 责任方挂 R-009-X）（2026-07-22）

**状态**：accepted

**确认来源**：用户 `/govern 响应 GOAL-009 A-057：关闭 F-029/F-030（刷新 Root/GOAL-012/workspace 现时叙述；I 责任方挂 R-009-X）`。

**决定**：

1. **关闭 F-029**（推荐 · 跨文档状态漂移）：刷新现时叙述，保留历史快照标注。  
   - [GOAL-001 `00-meta`](../GOAL-001-main-vision/00-meta.md)：阶段 6 现时表、当前阶段状态、6A–6X 路线图、当前子目标指向。  
   - [GOAL-012 `00-meta`](../GOAL-012-first-slice-workspace-detail/00-meta.md)：现时门禁节 + 历史关门句标注。  
   - [workspace.md](../workspace.md)：Web 写双门闩与 R-009-X 扩展门禁。  
2. **关闭 F-030**（推荐 · I/residual 责任方）：GOAL-009 信息表中仍 collecting 的扩展项（I-001/I-002/I-005/I-008～I-011 等）**责任方改为 R-009-X**；GOAL-009 仅作规划来源与有界关闭证据。  
3. **不**改 GOAL-009 `status`/`progress`（仍 done/100% 有界）；**不** verified 任何 I 全文；**不**关 Root；**不**宣称阶段 6 终态。  

**为什么**：A-057 independent pass 仅余文档卫生 recommended；用户明确授权关闭。

**影响**：A-058；02-execution；goal-tree 日志；上列路径。

## D-033 · R-009-X 下一扩展立项选项包（proposed · 待用户选定）（2026-07-22）

**状态**：proposed（**未**创建 GOAL-015+）

**确认来源**：用户 `/govern 按 R-009-X 审视下一扩展立项（X-NAV / X-SM / 试点`。

**决定（本拍仅审视 + 选项冻结，不立项）**：

1. **X-AI 已交付**：GOAL-014 `done / 100%`（有界）；不重开。  
2. **R-009-X 仍 accepted**；扩展责任在 R-009-X；GOAL-009 仍 `done / 100%` 有界。  
3. 下一拍候选（`parent: GOAL-001-main-vision`，规划来源 GOAL-009 / R-009-X；下一编号 **GOAL-015** 起）：

| 代号 | 建议 id | 主要 I / residual | 与 α / X-AI 关系 | 规模提示 |
|------|---------|-------------------|------------------|----------|
| **X-NAV** | `GOAL-015-n1-workspace-navigation` | I-009；R-F003-1～2；I-011 列表/选择 | α 为单配置绑定；补多区列表/选择/归档导航；禁止第二真相源 | 中；可有界 α-NAV |
| **X-SM** | `GOAL-016-shared-materials-product` | I-010；R-F004-1～3 | 一期产品规则已有；补用户 CRUD/引用/删除提示；AI 读依赖 X-AI 边界 | 大；须高层路线图 |
| **X-PILOT** | `GOAL-017-human-pilot-feedback` | I-007；I-012；原 R-E-2-H | 人类多会话使用反馈；可与实现并行；不交付导航/资料产品 | 中小；以证据清单为主 |
| **X-DEPLOY** | （后置） | I-005；R-F008 复审相关 | 首次对外/非本机部署前 | 本轮不默认 |

4. **组合选项**（用户一键选）：

| 组合 | 含义 | 建议 |
|------|------|------|
| **E1（推荐）** | 仅先立 **X-NAV** | 多工作区是已定产品模型；α 最大 UX 缺口是「只能配置绑一个区」；资料隔离正反测也更易在有导航后做 |
| **E2** | 仅先立 **X-SM** | 若优先兑现「一期必备共享资料」产品规则，先于 N1 |
| **E3** | 仅先立 **X-PILOT** | 先用 α+X-AI 做人多会话反馈，再定实现序 |
| **E4** | **X-NAV + X-PILOT** 并行 | 导航实现 + 人类反馈同时进行 |
| **E5** | **X-NAV + X-SM** | 产品面双扩展；工作量大，各目标须分阶段路线图 |
| **E0** | 本拍不立项 | 仅保留本选项表 |

5. **硬约束**：创建时写高层路线图（P-001）；**不**因立项将 I-009/I-010 标 verified；**不**宣称阶段 6 终态；**不**关 Root；材料/导航实现仍须 fail-closed 与用户确认。

**推荐理由（E1）**：X-AI 已补愿景缺口；下一最能解锁「多工作区」故事且范围可控的是 N1 导航；X-SM 更大且受益于可切换工作区后的隔离验收；X-PILOT 可随时并行但不替代产品缺口。

**未选（本拍）**：立即创建 GOAL-015+；批量堆砌三扩展；X-DEPLOY 优先。

**影响**：A-059；02-execution；goal-tree 日志。用户确认后如 `OK E1` 再创建。

## D-034 · 接受 E1：创建 GOAL-015（X-NAV）（2026-07-22）

**状态**：accepted

**确认来源**：用户在 A-059 后选择 **E1**：仅先立 X-NAV。

**决定**：

1. 创建 [GOAL-015-n1-workspace-navigation](../GOAL-015-n1-workspace-navigation/00-meta.md)，扩展代号 **X-NAV**，`parent: GOAL-001-main-vision`，规划来源本目标 / **R-009-X**。  
2. GOAL-009 保持 **`done / 100%` 有界**；**R-009-X 仍 accepted**（N1 由 GOAL-015 交付，不自动关 residual 全文）。  
3. **未**创建 X-SM / X-PILOT / X-DEPLOY。  
4. **不** verified I-009 全文；**不**宣称阶段 6 终态；**不**关 Root。

**为什么**：E1 优先补多区导航产品表面；资料与试点可后置。

**影响**：goal-tree 新增 GOAL-015；A-060。
